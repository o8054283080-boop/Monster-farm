# モンスターファーム デッキサバイバル - 開発引き継ぎメモ

最終更新: 2026/08/03
このメモは Claude Code (claude.ai/code のクラウドセッション) に開発を移行するための引き継ぎ資料です。
新しいセッションの冒頭でこのファイルを参照させれば、経緯を再説明せずに再開できます。

---

## 1. プロジェクト概要

ローグライク・デッキ構築ゲーム(モンスター育成 × カードバトル)

- 公開URL: https://o8054283080-boop.github.io/Monster-farm/
- GitHubリポジトリ: `o8054283080-boop/Monster-farm`
- ホスティング: GitHub Pages(ホーム画面に追加して利用)
- 難易度: ノーマル / ハード / エキスパート / ベリーハード / レジェンド の5段階
- 階層: 60階(ベリーハードのみ63階のレジェンドボスラッシュあり)
- プレイヤー種族: 7種族(モッチー / ゴーレム / モノリス / カワズモー / ガリ / ヒノトリ / ザン)

---

## 2. ファイル構成

### ゲーム本体・データ

| ファイル名 | 役割 |
|---|---|
| `index.html` | ゲーム本体(HTML/CSS/ロジック)。メインファイル。約1.4MB |
| `data-species.js` | 種族(自分のモンスター)データ。画像base64込み |
| `data-cards.js` | カードデータ |
| `data-relics.js` | 遺物データ(`isRare:true`フラグ付きのレア遺物が56種中10種) |
| `data-enemies.js` | 敵・ボスデータ(画像込み) |
| `data-skins.js` | **新規** スキンデータ(SR21体+SSR9体=30体)。約7.2MB |
| `data-accessories.js` | **新規** アクセサリデータ(14種) |
| `data-auras.js` | **新規** オーラのメタデータ(5種、見た目は未実装) |

### 音声・画像(リポジトリ直下)

- BGM: `bgm-menu.mp3` / `bgm-battle-normal.mp3` / `bgm-battle-elite.mp3` / `bgm-battle-elite2.mp3` / `bgm-boss-mid.mp3` / `bgm-boss-rush-1〜3.mp3` / `bgm-victory.mp3`
- SE: `se-select.wav`(選択音) / `se-damage.wav`(ダメージ音)
- 画像: `bg-dungeon.webp`(タイトル・マップ背景) / `title-logo.webp` + `title-logo.png`(ロゴ)

### 運用ルール

- **変更・追加があったファイルだけ**をアップロードすればOK
- `index.template.html`(単一ファイル版): データ4〜7ファイルを`index.html`に埋め込んだ動作確認用。GitHubへのアップロード不要。
  - **制約**: SEは`fetch()`で別ファイルを読む実装のため単一ファイルには埋め込めない(合成音にフォールバック。仕様上の制約でバグではない)
- 音声・画像を追加する時は、AudioContext/GainNode経由で音量調整できる仕組みに乗せる必要があるため、コード側の対応もセットで行う

---

## 3. 実装済みの機能(既存)

### サウンド

- BGMシステム一式(場面・階層・モードに応じて自動切り替え)
- SE差し替え(Web Audio APIの`AudioBuffer`で事前デコードし体感遅延をなくしている)
- 音量調整パネル(BGM音量・SE音量を別々に調整可能)
- **iOSは`<audio>`要素の`.volume`を無視する仕様のため、Web Audio APIのGainNode経由で音量制御している**(これが正解の実装方式)

### 最近の修正(このセッションで対応済み)

1. **戦闘勝利時にダメージSEが残る不具合**: `playSeBuffer()`で再生中のバッファを追跡し、`stopAllSe()`で`winBattle()`冒頭に強制停止
2. **ボス遺物選択画面に勝利BGMがない**: `showBossRelicChoice()`に`playVictoryBGM()`を追加
3. **モンスター選択の説明文とステータスのズレ**: `data-species.js`のdesc内「ライフ◯◯」を実際の値に修正(モッチー110 / ゴーレム100 / モノリス65 / カワズモー150 / ガリ75)
4. **手札が4枚しか見えない**: 手札の枚数に応じてカードサイズを自動縮小(`data-density`属性で`sm`/`xs`を切り替え)
5. **ダメージ予測が実際と違う**: `calcPreviewDmg()`を実際のダメージ計算式と完全に一致するよう全条件分岐を反映。敵の攻撃予告側も炎上/出血/氷結時の0.9倍系遺物を反映
6. **上部バーの整理**: 2段構成に変更(1段目=ステータス、2段目=遺物欄+🎴デッキ一覧+音量+ギブアップ)
7. **低い画面の端末対応**: `@media (max-height: 700px)`でコンパクトレイアウトに自動切り替え

---

## 4. 継承ショップ(メタ通貨システム) — 実装済み

序盤の理不尽さで離脱するのを防ぐための、周回で貯まる永続強化システム。

### 継承ポイントの入手

```
継承ポイント = 到達階数 × 3 × 難易度倍率
難易度倍率: ノーマル1.0 / ハード1.2 / エキスパート1.5 / ベリーハード1.75
+ ボス到達ボーナス: 15階+20 / 30階+40 / 45階+60 / 60階+100 / 63階+150
```

冒険終了時(全クリ/敗北/ギブアップの3パターン全て)に自動付与し、結果画面に表示。

### ショップの内容(タイトル画面「🏯 継承ショップ」から)

| 項目 | 効果 | 価格(段階ごと) |
|---|---|---|
| 開始ライフ+5 (×10段階) | 最大+50 | 40/55/75/100/130/165/205/250/300/360 |
| 開始チカラ+3 (×5段階) | 最大+15 | 60/110/180/270/380 |
| 開始丈夫さ+3 (×5段階) | 最大+15 | 60/110/180/270/380 |
| ガッツ回復量+1 (×5段階) | 最大+5 | 80/150/240/350/480 |
| ガッツ上限+4 (×5段階) | 最大+20 | 70/130/200/290/400 |
| 初期遺物解放 | 冒険開始時に遺物を1つ持てる | 1000(買い切り) |
| SSRカード確率アップ (×3段階) | SSR出現率 +0.5% / +1.0% / +1.5%(累計) | 500/1000/2000 |
| MRカード確率アップ (×3段階) | MR出現率 +0.25% / +0.5% / +1.0%(累計) | 750/1500/3000 |
| レア遺物確率アップ (×3段階) | isRare遺物の出現率 +2% / +4% / +7%(累計) | 700/1400/2500 |
| 初期ゴールド+15 (×10段階) | 最大+150 | 50/75/100/300/500/800/1100/1400/1700/2000 |
| 取得ゴールドアップ (×5段階) | 入手ゴールド +5%〜+25%(累計) | 500/1000/1500/2000/2500 |
| 初期手札+1 (×1段階) | 戦闘開始時の手札が1枚増える | 3000(買い切り) |
| 不屈 | 1冒険に1度だけ、ライフ0でも最大ライフの30%で復活 | 3500(買い切り) |

**取得ゴールドアップの注意**: ゴールドの加算は必ず`gainGold()`を通すこと。
`state.gold += n`と直接書くと倍率が乗らず、項目が効かなくなる。

**不屈の注意**: ヒノトリの種族特性の復活とは**別枠**なので、ヒノトリなら2回復活できる。
`gameOver()`の中で種族特性→不屈の順に判定している。

### 初期遺物解放(1000pt)— 実装済み

購入済みなら、**種族選択の直後に候補3つが提示され、1つ選んで持ち込める**(`showStarterRelicChoice()`)。
1000ptと高価な買い切りなので、ランダムで1つ渡すより選べた方が納得感があるという判断。

候補は`STARTER_RELIC_POOL_IDS`(12種)。**idだけを持ち、実データは`ALL_RELICS`から引く**ので
遺物の性能を調整してもこちらを直す必要はない(データ二重管理による不整合を避けるため)。

| 系統 | 候補 |
|---|---|
| 万能 | カニのはさみ(力+3 丈夫さ+5) |
| 攻め | パンチグローブ(力+3 初期ガッツ+5) / ネンドロシール(ライフ25 力4) |
| 守り | 鉄の盾(丈夫さ+3 初手ブロック10) / だんろ石(常にブロック3) / アルテミス像(毎ターンブロック2) |
| ガッツ | ガッツ豆(毎ターン+2) / ピクシーの羽(初期ガッツ+10) / ガッツミン(上限+10) |
| 耐久 | 白銀モモ(ライフ+25) / 双子の水差し(毎ターンライフ2回復) |
| 長期 | 秘伝の書(ランダムに1枚強化) |

**意図的に外しているもの**: レア遺物(`isRare`)とボス遺物(冒険中に見つける楽しみを残すため)、
種族専用(`mid`付き。種族ごとの当たり外れが出るため)、ゴールド系(序盤の生存に繋がらないため)、
黄金モモ(ライフ+50)やグジラシール(力6)など強すぎるもの(序盤が作業になるため)。

候補を増減したい時は`STARTER_RELIC_POOL_IDS`にidを足し引きするだけでよい。

### 保存

localStorageキー `mf_meta_progress`。既存のセーブ・ランキングとは独立。

---

## 5. ダイヤ通貨システム — 実装済み

ガチャ専用のレア通貨。localStorageキー `mf_dia_progress`。

| 入手方法 | 内容 |
|---|---|
| ログインボーナス | 毎日15、5日連続ごとに+50 |
| **初回限定ウェルカムボーナス** | 初めて起動した時のみ+300(`welcomeBonusClaimed`フラグで管理) |
| 累計プレイ回数 | 1/10/30/50回 → 100/100/150/200、以降30回ごとに250(無期限) |
| 階層到達(毎回) | 15階 3/4/5/7・30階 5/7/9/12・45階 8/11/14/18・60階 15/20/26/35(難易度別) |
| 63階(ベリーハードのみ) | +50 |
| 種族別初踏破称号(1回のみ) | 60階以上到達で種族×難易度ごとに1回 50/70/90/120(63階なら150) |

タイトル画面の右上に固定バッジで所持数を表示。

### 称号画面(タイトル画面「🎖️ 称号・挑戦回数」)

回数報酬のダイヤが何の説明もなく増えて分かりにくかったため追加した画面(`window.game.showTitles()`)。

- **通算の挑戦回数**と、次の回数報酬が何回目でいくら貰えるか
- **回数報酬の一覧**(達成済み/未達成。`listDiaPlayMilestones()`が直近4件+次の2件を返す)
- **種族別の初踏破称号**(7種族 × 4難易度 = 28個)。難易度ごとに格が変わる
  (ノーマル=踏破者 / ハード=猛者 / エキスパート=達人 / ベリーハード=覇者)

あわせて、**回数報酬を貰った瞬間にトーストで通知**するようにした(節目でない回は通知しない)。
`registerDiaPlayCount()`は以前から獲得量を返していたが、呼び出し側が捨てていたため無言で増えていた。

---

## 6. ガチャ — ロジック実装済み・演出は未実装

localStorageキー `mf_gacha_progress`。タイトル画面「💎 ガチャ」から。

### コスト

単発 💎15 / 10連 💎135(1割引)

### 排出率

| 枠 | 確率 | もらえるもの |
|---|---|---|
| ハズレ | 63% | 継承ポイント20 |
| レア・大量継承pt | 10% | 継承ポイント150 |
| レア・アクセサリ | 15% | アクセサリ1個 |
| SRスキン枠 | 8% | SRスキン または モーション(半々) |
| SSRスキン | 2% | スキン(SSR) |
| ウルトラレア・オーラ | 2% | オーラ1種 |

### 天井

- 10連に1回、レア(アクセサリ)以上確定
- 累計100連ごとに、SRスキンを選んで入手
- 累計200連ごとに、SSRスキンかオーラを選んで入手
- **注意**: SR天井とSSR/オーラ天井は独立カウント。200連目・400連目は**両方同時に発火**する(実装済み・検証済み)

### 被り(重複)時の還元

| 種別 | 継承ポイント |
|---|---|
| SRスキン | 100 |
| SSRスキン | 400 |
| アクセサリ | 50 |
| オーラ | 400 |

### 抽選範囲

スキンは**全7種族×全スキンから完全ランダム**(自分が育てていない種族のスキンも出る)。

提供割合の画面(`showGachaRates()`)の「全◯種」は`allSkinsOfRarity()`や`ACCESSORIES.length`から
数えているので、スキンやアクセを増やしても表示は勝手に追従する(以前は手書きの数字でズレる元だった)。

---

## 7. コスメ(スキン・アクセサリ・オーラ)

localStorageキー `mf_cosmetics`。

```js
{
  ownedSkins: [],           // 所持スキンidの配列
  equippedSkin: {},         // {種族id: スキンid}
  ownedAccessories: {},     // {アクセサリid: {hue, rotate, offsetXPct, offsetYPct, scale}}
  equippedAccessories: { hat:null, mask:null, other:null, weapon:null },
  ownedAuras: [],
  equippedAura: null,
}
```

### スキン(30体・全画像登録済み)

各種族 SR3体 + SSR1体以上(モノリスとゴーレムはSSR2体)。ユーザー提供の高クオリティ一枚絵を丸ごと差し替える方式。
(当初検討していた「主要色検出による色置換」方式は**不採用**。画像支給前提に変更した)

種族ごとの命名例: モッチーSR「夜霧の/氷雪の/漆黒の宝玉」、SSR「聖天使モッチー」など。

### スキン専用の初期技(SSRスキン9体)

各SSRスキンに、その種族の看板技(初期デッキに1枚だけ入っている`*_b3`)の差し替え版が紐づいている。
**SSRは種族ごとに1体という決まりではない**。同じ`*_b3`を差し替える専用技が複数あってもよく、
装備画面の初期技リストは`getSkinStarterCards()`が種族のスキン全部を見て自動で並べるので、
SSRを1体足すたびに `SKINS` と `BASE_CARDS` の2箇所に足すだけでよい。

| 種族 | 通常 | 専用技(解放スキン) |
|---|---|---|
| モッチー | さくらふぶき 18/攻15 守弱2 | **天使の舞** 13/攻10 守弱2 回2(聖天使モッチー) |
| ゴーレム | 大パンチ 18/攻23 | **氷晶の鉄拳** 16/攻21 氷結1(氷晶装甲ゴーレム) |
| ゴーレム | 〃 | **スピリットパンチ** 12/攻28 攻弱2 クリC(スピリットゴーレム) |
| モノリス | おんぱ 12/攻12 攻弱2 | **翠玉の頁** 12/攻11 攻弱2 次ガッツ5(翠玉の禁書) |
| モノリス | 〃 | **魔王の雄叫び** 20/攻35 氷結1 感電1 クリE(魔王の魔典) |
| カワズモー | がっぷりよつ 21/攻22 守弱2 | **がっぷりよつ蒼** 15/攻17 守弱2 防8(宝玉の蛙王) |
| ガリ | レッドウィスプ 22/攻14 攻弱守弱2 | **深淵の焔** 22/攻12 攻弱守弱2 炎上2(深淵の陽衣) |
| ヒノトリ | 爆裂落とし 27/攻38 | **黄金雷撃** 27/攻32 感電2(黄金雷鳥) |
| ザン | 血踊 10/以降の攻撃に出血+1 消滅 | **溶岩の型** 13/炎上2 以降の攻撃に出血+1 消滅(溶岩装甲の忍) |

**解放条件はスキンの「所持」だけで、装備は不要**。装備を条件にすると見た目がSSR一択に
なってしまうため、意図的に切り離してある。装備画面の「初期技」欄から種族ごとに選び、
`mf_cosmetics`の`starterCards`(`{種族id: カードid}`)に保存される。

**重要**: 専用技は`BASE_CARDS`にだけ定義し、**`CARDS`には入れないこと**。
カード報酬・ショップ・イベントの抽選は全て`Object.values(CARDS)`から作られているので、
`CARDS`に入れるとスキン未所持でも入手できてしまう。
カード自体が`skinCardOf`(解放スキンid)と`replaces`(置換対象のカードid)を持っているので、
スキンを増やす時はカードを1枚足すだけでUIも自動で増える。

### アクセサリ(14種)

4ジャンル、各ジャンル1スロットずつ**最大4個同時装備**。

| ジャンル(内部id) | 内容 |
|---|---|
| 帽子 (`hat`) | 王者の兜 / 探偵帽 / ミニシルクハット / フローラルハット |
| メガネ・仮面 (`mask`) | ゴールドモノクル / ハイテクバイザー / オニキス仮面 / キャットアイマスク |
| その他 (`other`) | 浮遊式メカニカル・アーム・ユニット / 従者ミニオン(ちびゴーレム) / 紋章付き装甲プレート |
| 武器・小物 (`weapon`) | 聖なる盾 / エネルギーブレード / 従者スライム |

**座標システムは%ベース**(重要):
`pos: {種族id: {xPct, yPct, wPct, hPct, rotate}}` — キャラの表示サイズに対する割合で持つ。
これにより装備画面のプレビュー(160px枠)でもバトル画面(画面高さで67px〜88pxに変動)でも同じ位置に揃う。
ピクセル固定にすると画面ごとにズレるので**絶対に%ベースを維持すること**。

プレイヤーは装備画面で以下を自由にカスタマイズ可能(アクセサリごとに個別保存):
- 色相(`hue`、0〜359°、CSS `hue-rotate`)
- 角度(`rotate`、0〜359°)
- 大きさ(`scale`、30〜250%)
- 位置(`offsetXPct`/`offsetYPct`、プレビュー内をドラッグして調整)

**装備も調整値も種族ごとに別々に保存される**(`mf_cosmetics`)。

- `equippedAccessories`: `{種族id: {hat,mask,other,weapon}}`
- `accessoryTuning`: `{種族id: {アクセサリid: {hue,rotate,offsetXPct,offsetYPct,scale}}}`
- `ownedAccessories`: 所持の記録(旧形式の名残で調整値も残っているが、表示には使わない)

以前は全種族で共通だったため、体格の違う種族に持ち替えると位置がズレていた。
旧データは`migrateAccessoryData()`が**全種族に同じ設定をコピーする**形で引き継ぐので、見た目は変わらない。

**注意**: 調整値の書き込みは必ず`updateAccTuning()`を使うこと。
この関数は内部で`loadCosmetics`→`saveCosmetics`まで行うので、
呼び出し側で先に読んだ`c`を後から`saveCosmetics(c)`すると**変更が消える**。

### オーラ(5種・実装済み)

| id | 名称 | 実装した表現 |
|---|---|---|
| `aura_halo` | 光輪のオーラ | 楕円グロー+ふちの淡いリングがゆっくり明滅 |
| `aura_holy` | 神々しき後光 | 背後の金/紫の光+ゆっくり回る光条。キャラ本体は透過アルファ形状に沿ってdrop-shadowで発光 |
| `aura_flame` | 業火の足跡 | 足元の残り火+縦に伸びながら立ちのぼる炎の粒子6個 |
| `aura_rainbow` | 幻虹のオーラ | 回転する虹色リング(conic-gradient+maskのドーナツ)+きらめく粒子6個 |
| `aura_king` | 覇王の闘気 | 放射状のトゲトゲの闘気(repeating-conic-gradientで生成)が回転+明滅 |

画像素材は一切不要。**CSSのグラデーションのみ**で全7種族に使い回せる(当初SVGを想定していた覇王の闘気も、
`repeating-conic-gradient`+`mask`で同じ絵が作れるためDOMを増やさずCSSだけで実装した。いずれも独自実装で著作物不使用)。

**呼び出し漏れに注意**: プレイヤーのスプライトを組み立てる場所は
`renderBattleSprites()`(冒険の再開時)と`window.game.selectMonster()`(通常の開始時)の**2箇所ある**。
`renderAura()`が片方にしか無く、**普通に始めた冒険ではオーラが最後まで出ない**不具合が起きた。
`renderBattleAccessories()`を呼んでいる場所には`renderAura()`もセットで置くこと。

**構造**: `#battle-aura-fx`(バトル)と`#loadout-aura-fx`(装備プレビュー)を**静的に**置いてある。
中身は`.aura-layer-a` / `.aura-layer-b` / `.aura-p`(粒子6個)の固定8要素で、
どれを使うか・どう見せるかは全て`.aura-fx[data-aura="..."]`のCSS側が持つ。
JS(`renderAura()`)は**`data-aura`属性と表示/非表示を切り替えるだけ**で、DOMを動的に作らない。

- サイズは全て%指定なので、バトル画面(5.5rem/4.2rem)でもプレビュー枠(160px)でも同じ見た目になる
- オーラを増やすときは `data-auras.js` に1行足して、CSSに`[data-aura="新id"]`のブロックを足すだけでよい

### カスタム装備画面のレイアウト

縦に長すぎて何度もスクロールが必要だったため、**プレビューの左右に選択枠を寄せる**形に変更した。

```
[帽子][メガネ]   [ プレビュー144px ]   [オーラ]
[その他][武器]      モンスター          タップで
                   +アクセ+オーラ       一覧を開く
```

- 左列 `#loadout-accessory-slots` … **2列×2行**のグリッド。タップで従来のアクセサリ選択モーダル
- 右列 `#loadout-aura-slot` … 1枠だけ。`openAuraPicker()`で`#aura-picker-modal`(なし+5種)を開く

**左右の列は `flex-1 min-w-0 max-w-[104px]` で同じ幅**にしてある。プレビューだけ`shrink-0`なので、
画面幅が変わっても**プレビューは必ず画面の中央**に来る(360px〜430pxで中心ズレ0pxを確認済み)。
枠の大きさは画面幅に応じて41〜49pxに伸縮する。

オーラ枠の幅 `w-[calc(50%_-_3px)]` は「アクセ枠1マス」と同じ大きさになるよう計算したもの
(`gap-1.5`=6pxの半分を引いている)。**`renderLoadoutAuraSlot()`が`className`を丸ごと上書きするので、
HTML側のクラスを変えたらJS側も必ず同じに直すこと。**
なおTailwindの任意値でcalcを書くときは`_`が半角スペースに変換される仕様を使っている
(`calc(50%-3px)`と詰めて書くとCSSとして不正になる)。
- 以前あった画面下部のアクセサリ一覧・オーラ一覧のセクションは**削除済み**(`renderLoadoutAuraGrid()`は
  `renderLoadoutAuraSlot()`に置き換わった)
- プレビュー枠が小さいためオーラがはみ出す。`#loadout-aura-fx { transform: scale(0.78); }`で内側に収めている

画面の高さは 2159px → 1694px(約22%減)。

---

## 7.5 カードの見た目と操作

### 彫金デザイン(説明文は面から外した)

カード面は「コスト / 名前 / 大きな数値 / 会心・役割」だけにして、**説明文は長押しのポップアップへ移した**。
面の下40%を占めていた極小の説明(6〜8px)が無くなったぶん、名前と数値を大きくしている。

- **種族** … カードの地の色そのもの + 名前の帯(`--spBand`) + 下辺のライン = CSS変数 `--sp` / `--spBand`
  (7種族 + 共通 + 妨害の9通り。**`bg-zan`を足し忘れるとザンが共通色になる**ので注意)
  - 色は`createCardUI`が`c.mid`(種族id)を見て決める。**`mid`が無いカードは共通色になる**。
    初期デッキ用の`BASE_CARDS`は元々`mid`を持っておらず、初期カードが全部共通色になっていたので
    後から追加した。カードを足すときは`mid`の付け忘れに注意(共通の防御・ためるは意図的に付けていない)
- **役割** … 中央の光・背後の漢字の透かし・右下のラベル = CSS変数 `--tp`
  (中央は「暗い下地→役割色」の二層にしてある。種族の地の色が明るいと役割色が埋もれるため)
  (攻撃=赤 `攻` / 防御=青 `守` / 特殊=紫 `特` / 妨害=紫 `妨`。判定は `cardRoleOf()`)
- **レア度** … 枠の色と外側の光(`.rarity-*`)。右上のランク章の色もここで `--rankC/--rankB` として渡している

**注意点**:
- `.card`側で`box-shadow`を使ってはいけない。後方の`.rarity-*`の`box-shadow`に丸ごと上書きされて消える
- 透かしに絵文字(⚔🛡)を使うと`color`が効かず(カラー絵文字として描かれる)役割色で塗り分けられない。**漢字を使うこと**
- 役割色は`color-mix()`を使わず実色のrgbaで持たせている(古いiOSでも確実に出すため)
- 手札が増えると`data-density`が`sm`→`xs`と切り替わる。狭いほど会心/役割の行→ランク章の順に落として、
  **コスト・名前・数値だけは必ず読める**ようにしてある

### 長押しで説明を表示

**手札のカードは「長押し(200ms)＝ドラッグ開始」が既に埋まっている**ため、素直に長押しを割り当てられない。
そこで「長押ししてドラッグ状態にした後、**動かさずにそのまま離す**」(＝これまで何も起きなかった操作)に
説明表示を割り当てた。既存の操作は一切変えていない。

| 操作 | 結果 |
|---|---|
| タップ | カードを使う(従来通り) |
| 長押し → 敵へドラッグ | カードを使う(従来通り) |
| 長押し → 動かさずに離す | **説明を表示(新規)** |

手札以外(デッキ一覧・カード報酬・ショップ・修行)はドラッグしないので、500msの素直な長押しで出す。
指が12px以上動いたら中止するので、一覧のスクロールは妨げない。
カード要素には`__cardData`でカード本体を持たせてあり、ポップアップはそこから内容を引く。

---

## 7.6 難易度レジェンド

ベリーハードの上の最高難易度。

| 項目 | 内容 |
|---|---|
| 敵ステータス | **1.5倍**(HP・攻撃力とも。`startBattle`と`setEnemyIntent`の両方に倍率がある) |
| 最終スコア | **2.25倍** |
| 継承ポイント | **2.25倍**(スコア倍率と揃えている) |
| ダイヤ(階層到達) | 15階9 / 30階16 / 45階24 / 60階45 |
| 初踏破称号 | 170(63階なら220)。称号の格は「伝説」 |
| レジェンドボスラッシュ | ベリーハードと同じく解放される |

### 呪い(初期デッキに1枚)

`CURSE_CARD` … コスト0・効果なし・**デッキから取り除けない**。レジェンドでのみ初期デッキに1枚入る。

取り除けないことは`noRemove:true`で表現していて、以下の全てで除外している。
**カードを減らす処理を新しく足すときは、必ず`noRemove`を見て弾くこと。**

- 修行(削除) / ショップの削除
- 落とし穴イベント
- 合成(2枚を消費する)の候補
- 修行(強化) / ランダム強化の候補(強化しても無意味で枠を埋めるだけのため)

### 大ケガ

`BIG_INJURY_CARD` … コスト30・**ターン終了時に手札に残っていると自身に10ダメージ**(`handEndSelfDmg`)。

**レジェンド難易度かつレジェンドボスラッシュ中(61階以降)のときだけ**、
`addInjuryCards()`が通常のケガの代わりにこれを山札へ挿し込む。

自傷の判定は`handleEndTurn()`の冒頭、**手札を捨てる前**に行っている(捨てた後だと判定できない)。
ダメージは`applySelfDamage()`を通しているので、身代わり人形による軽減や死亡判定もそのまま働く。

---

## 7.7 カード演出モーション(15種)

カードを使ったときの演出。**5場面 × 3種 = 15種**。ガチャのSR枠で入手し、装備画面で場面ごとに選ぶ。

| 場面 | 発火するタイミング | 出る場所 | 候補 |
|---|---|---|---|
| 攻撃 | ダメージを与えたとき | 敵 | 斬撃 / 爆砕 / 連閃 |
| 防御 | ブロックを得たとき | 自分 | 六角障壁 / 石壁 / 光輪盾 |
| 強化 | 力・丈夫さ等が上がったとき | 自分 | 上昇気流 / 紋章 / 覚醒 |
| 弱体 | 相手に弱体を与えたとき | 敵 | 呪縛 / 侵食 / 亀裂 |
| 回復 | ライフが回復したとき | 自分 | 生命の雫 / 聖光 / 再生 |

**「一体型」**なので、エフェクト(`fxOn`)と**キャラの動き**(`move` = `mv-*`クラス)が1セットになっている。
定義は`CARD_MOTIONS`にまとまっていて、増やすときはここに1行足してCSSを書けばUIも自動で増える。

### 実装上の注意

- **キャラの動きは必ず`#player-visual-wrap`に当てる**。スプライト単体(`#player-visual`)を動かすと
  アクセサリとオーラが置いていかれる。wrap側の`idle`アニメより優先させるためID込みのセレクタにしてある
- **1枚のカードが複数の場面に当てはまることがある**(攻撃しつつ弱体を与える等)。同じ対象に重ねると
  後の演出が前を消すので、`__cardMotionUsed`で**対象(敵/自分)ごとに1つだけ**出すようにしている。
  キャラの動きも1枚につき1回だけ。呼ばれる順が 攻撃→防御→強化/回復→弱体 なので、攻撃カードなら攻撃の演出が残る
- エフェクトは敵/自分の**スプライト**(`ui.enemyVisual` / `ui.playerVisual`)に差し込む。
  ノード全体に差すとHPバーや攻撃予告の上にも出てしまう
- 未所持のモーションを装備データが指していても再生しない(`pickMotionForCat`で所持を確認している)

### 入手と装備

- ガチャの**SR枠(5%)を引くと、SRスキンかモーションが半々**で出る。被りは継承ポイント100(SRスキンと同じ)
- 100連天井の選択肢にもモーションが並ぶ
- 装備は`mf_cosmetics`の`ownedMotions`(所持)と`equippedMotions`(`{場面key: id | 'random'}`)
- **ランダム**を選ぶと、その場面で所持しているものから毎回抽選される。未設定なら演出なし(従来通り)

---

## 7.8 その他の画面

### 提供割合(ガチャ画面「📊 提供割合を見る」)

`showGachaRates()`。表示は`GACHA_RATES`から組み立てているので、**確率を変えれば表示も自動で追従する**。
ただし`rollGachaTier()`は合計値で正規化するため、**合計が100%から外れると表示と実際がズレる**。
確率をいじるときは必ず合計を1.00に保つこと。

### 更新履歴(タイトル画面左下「最終アップデート ▸」をタップ)

`CHANGELOG`配列を`window.game.showChangelog()`が表示する。**先頭が最新**で、先頭の1件だけNEWが付く。
新しい版を出したら配列の先頭に1件足すだけでよい。

### 敵スプライトの表示倍率

`ENEMY_SPRITE_SCALE`(trait → 倍率)。
敵画像は`6rem × 7rem`の**縦長枠**に`object-fit:contain`で収めているため、
**横長の画像は縦が余って小さく見える**。ビークロン(360×269の横長)がこれで、1.4倍を指定して解決した。
同様に小さく見える敵が出たら、この表からtraitと倍率を足す。

### 音量ボタンの重なり

`#global-volume-btn`は画面左上に固定表示(z-190)。カスタム装備画面の「← 戻る」と重なるため、
`openLoadout`で隠し`closeLoadout`で戻している。**左上にボタンを置く画面を増やすときは同じ対応が必要**。

---

### 記念配布などの一括配布

`UPDATE_GIFT_KEY` / `UPDATE_GIFT_AMOUNT`。localStorageのフラグを見て**1端末につき1回だけ**配る。
次回また配るときは、**キーの日付部分を変えて**金額を書き換えるだけでよい(キーを変えないと配られない)。
ログインボーナスのトーストと重ならないよう、0.9秒ずらして表示している。

---

## 8. 実装上の重要な注意点(ハマりどころ)

このセッションで実際に踏んだバグと、その教訓。

### TDZ(一時的死角)によるサイレント失敗

`const`で宣言した定数は、その行に処理が到達するまで参照できない。
ページ読み込み時に即実行されるIIFE(ログインボーナスのチェックなど)が、
後方で定義された`const DIA_STORAGE_KEY`を参照して**静かに失敗**していた。

→ 対策として、`window.game = {}` の直後(スクリプト最上部)に4つのストレージキーを全て定義済み:
```js
const DIA_STORAGE_KEY = 'mf_dia_progress';
const META_STORAGE_KEY = 'mf_meta_progress';
const COSMETICS_STORAGE_KEY = 'mf_cosmetics';
const GACHA_STORAGE_KEY = 'mf_gacha_progress';
```
**この位置から動かさないこと。**

### 非表示要素の`offsetWidth`は0になる

バトル開始時、まだ`display:none`の状態でスプライトのサイズを測ろうとして0になり、
アクセサリが非表示になるバグがあった。
→ 実測をやめ、CSSのブレークポイント(`max-height: 700px`)から表示サイズを直接計算する方式に変更済み。

### データファイルの差し替え漏れ

`index.html`だけ更新して`data-accessories.js`を古いままにすると、新コードが旧形式データを読んで
`NaN`になり「スライダーを動かしても何も変わらない」という分かりにくい症状になる。
**データ構造を変えたら、必ず該当データファイルも一緒に差し替えること。**

### try/catchが握りつぶすReferenceError(修正済み)

`playCard()`のクリティカル発動効果が**全て無言で失われていた**。

原因は`let anyCritThisAttack = false;`が`if(c.val>0||...)`ブロックの**内側**で宣言されていたのに、
判定はブロックの**外側**で行われていたこと。参照した瞬間にReferenceErrorになるが、
その一帯が`try{...}catch(e){console.error(...)}`で囲まれているため、
**画面上は何のエラーも出ず、ただ効果が発動しないだけ**という状態になっていた。

これで死んでいたのは以下すべて(ドローだけでなく全部):
`critEnergy` / `critBonusDmg` / `critWeak` / `critBlock` / `critDraw` /
クリティカルバッジ / モストの鉢巻 / 根性飴 / スケスケメガネ / 風神の刃

→ 宣言を関数スコープへ引き上げて解決。
**教訓**: 効果が「なぜか発動しない」時は、まずコンソールを見ること。
このプロジェクトは効果処理を細かくtry/catchで囲んでいるので、
1箇所の例外が握りつぶされて静かに機能が消えることがある。

### position指定の無い要素にz-indexは効かない

`.monster-sprite`には`z-index:10`が指定してあったが`position`が無かったため**z-indexが完全に無視されていた**。
そこにオーラ(`position:absolute`)を重ねたところ、オーラがキャラの手前に来て顔が塗り潰される、という症状になった。

→ `#player-visual`に`position:relative`を追加して解決。**重ね順を指定したい要素には必ずpositionも付けること。**

なお、`.aura-fx`は`z-index:5`でキャラ(10)より奥・アクセサリ(20)より奥に置いているが、
**業火の足跡だけは`z-index:15`でキャラの手前**に出している(背面だと足元の炎がキャラに隠れて見えないため)。

### 冒険終了後も自動セーブが復活してしまう(修正済み)

`gameClear()` / 敗北 / ギブアップはいずれも `clearAutosave()` を呼んでいたのに、
**クリア後にタスクキルすると冒険データが残っていて再開できてしまう**という不具合があった。

原因は「結果画面(モーダル)を出している間も、裏のバトル/マップ画面は表示されたまま」であること。
`doAutosave()` は「バトルかマップが表示中なら保存する」判定なので、

1. `visibilitychange`(hidden) → タスクキル時に `doAutosave()` が直接呼ばれる
2. `scheduleAutosave()` の保留中タイマー(400ms)が `clearAutosave()` の後に発火する

のどちらの経路でも、**消したはずの `mf_active_run` が書き戻されていた**。

→ `__autosaveDisabled` フラグを追加し、`clearAutosave()` で保留タイマーの停止と保存の無効化を行うよう修正。
新しい冒険が始まる `resetGameState()` で `false` に戻して再開する。
**冒険終了系の処理を増やす時は `clearAutosave()` を呼べばよい**(フラグも一緒に立つ)。

### 空divをアンカーにした重ね合わせは不安定

アクセサリ表示で、空の`<div>`を作ってその中に動的に`<img>`を追加する方式は
ブラウザによって描画されないことがあった。
→ 静的な`<img>`要素を4つ用意しておき、JSでsrc/styleを差し替える方式に変更済み。

---

## 9. デバッグ用のURLパラメータ

| パラメータ | 効果 |
|---|---|
| `?floor=61` | その階層から開始 |
| `?boost=1` | ライフ・力・丈夫さ・ガッツ上限を底上げ |
| `?boss63=1` | 63階ボスに即挑戦(ライフ300/力丈夫さ60/ガッツ回復+50/MRカード全取得/カード全強化) |
| `?resetCosmetics=1` | 所持スキン・アクセサリ・オーラ・装備状態とガチャ天井カウントを全消去。**初回のみお詫びダイヤ+200を付与**(`mf_reset_apology_claimed`フラグで管理) |

---

## 10. 次にやること

### 優先度高

1. **⑤ ガチャ演出の実装** — ✅ 実装済み(このセッションで対応)
   - `#gacha-overlay`(index.html、`<main>`内・modal-overlayの直後)を新設。CSSは`<style>`内「ガチャ演出」セクション(`.gacha-*`クラス群)、JSは「ガチャ演出」セクション(`playGachaAnimation`が入口)
   - 待機画面: 二重の魔法陣リング(`.gacha-ring-outer`/`-inner`)が常時ゆっくり反対回転
   - 抽選演出: 加速回転(`is-charging`)→卵が震えて光漏れ(`is-shaking`)→パーティクル爆発+中身公開(`is-bursting`→`.gacha-reveal`)。タイムラインは`runSingleGachaReveal()`
   - レア度別の色/パーティクル量/画面フラッシュ有無/卵の見た目は`GACHA_ANIM_THEME`テーブルで管理(ハズレ〜オーラの6段階。SRは控えめフラッシュ、SSR/オーラは強いフラッシュ)
   - **卵の見た目もレア度で変わる**: SR=金の卵(`egg-gold`、光沢が回転) / SSR=虹の卵(`egg-rainbow`、色相が回転) / オーラ=虹+白く速く輝く(`egg-prism`)。中身が出る前から期待感が出るようにしたもので、10連のミニ卵にも同じクラスが付く。テーブルの`egg`フィールドを変えるだけで差し替え可能
   - **背景は専用の不透明背景**(`#gacha-overlay`のCSS)。深い紫のグラデーション+星屑で「召喚の間」風。ここを半透明にするとタイトル画面が透けて演出が締まらないので、不透明のままにすること
   - オーラ枠のみ全画面リング拡散演出あり(`.gacha-ring-burst`、CSS maskで円形に、`border-image`はborder-radiusを無視して四角くなるため不採用)
   - 10連は`runTenPullGachaSequence()`が10個のミニ卵を`.gacha-egg-grid`で順番に弾き、最後に今回最高レアだけ`runSingleGachaReveal()`を「★ トリ確定演出 ★」付きで再生
   - 演出中は`#gacha-overlay`タップで`window.game._gachaSkip()`が呼ばれ即座に結果画面へ
   - 結果画面(`showGachaResults`)に天井カウント(次のSR/SSR・オーラ確定まであと◯連)を表示済み
   - アクセサリ/スキンは`item.img`があるので画像表示、オーラは`img`フィールドが存在しないため✨アイコンにフォールバック(3箇所: 結果一覧・単発演出の公開カード・10連ミニ卵の中身表示)
   - Playwrightで単発/10連/スキップ/SSR/オーラの各演出を実際にレンダリングして確認済み(Tailwind CDNがサンドボックス外からのみ読み込めるため、確認時は`tailwindcss` CLIでローカルビルドした代替CSSに差し替えてスクリーンショット取得。本番のGitHub Pagesは通常通りCDNから読み込むので影響なし)

2. **⑥ オーラの戦闘中表示** — ✅ 実装済み(このセッションで対応)
   - 5種類すべてをCSSのみで実装し、バトル画面と装備プレビューの両方に反映。詳細は「7. コスメ」のオーラ節を参照
   - Playwrightで5種類×バトル/プレビューを実際にレンダリングして確認済み

### 優先度中

3. ~~**初期遺物解放の中身を決める**~~ — ✅ 実装済み(候補12種から3択。詳細は「4. 継承ショップ」参照)。
   実際に何周か遊んでみて、選択肢が弱すぎ/強すぎと感じたら`STARTER_RELIC_POOL_IDS`を調整する
4. **アクセサリの種族ごとの座標調整** — 現状は全種族共通の仮値。装備画面のドラッグ機能で種族ごとに詰める
5. **スキンと連動した専用カード** — 「スキンによって専用カードを入れたい」という構想あり(未設計)

### 保留・未確認

- 「全体サウンドをオン→オフ→オンにすると、メニューBGMが本来鳴らないはずの場面でも鳴ってしまう」という報告があったが、明示的な修正はまだ行っていない。再現するか確認が必要
- タイトル画面の「継承ショップ」内に、ダイヤの入手履歴や統計を見せる画面があってもよいかもしれない(未検討)

---

## 11. 開発時のお願い

- 変更後は必ず**JSの構文チェック**を通すこと(このプロジェクトは1ファイルが巨大なので、構文エラーがあるとゲーム全体が起動しなくなる)
- ロジックを変更したら、可能な範囲で**実際に実行して検証**すること。このセッションでは、天井ロジックを10万回シミュレーションして「200連目でSR天井が消える」バグを発見できた
- iOS Safari(iPhone)で動作確認されることが多い。ホーム画面に追加したPWAとしても使われる
