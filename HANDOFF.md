# モンスターファーム デッキサバイバル - 開発引き継ぎメモ

最終更新: 2026/08/03
このメモは Claude Code (claude.ai/code のクラウドセッション) に開発を移行するための引き継ぎ資料です。
新しいセッションの冒頭でこのファイルを参照させれば、経緯を再説明せずに再開できます。

---

## 1. プロジェクト概要

ローグライク・デッキ構築ゲーム(モンスター育成 × カードバトル)

- 公開URL: https://o8054283080-boop.github.io/Monster-farm/
- GitHubリポジトリ: `o8054283080-boop/Monster-farm`
- ホスティング: GitHub Pages(ホーム画面に追加して利用)
- 難易度: ノーマル / ハード / エキスパート / ベリーハード / レジェンド の5段階
- 階層: 60階(ベリーハードのみ63階のレジェンドボスラッシュあり)
- プレイヤー種族: 7種族(モッチー / ゴーレム / モノリス / カワズモー / ガリ / ヒノトリ / ザン)

---

## 2. ファイル構成

### ゲーム本体・データ

| ファイル名 | 役割 |
|---|---|
| `index.html` | ゲーム本体(HTML/CSS/ロジック)。メインファイル。約1.4MB |
| `data-species.js` | 種族(自分のモンスター)データ。画像base64込み |
| `data-cards.js` | カードデータ |
| `data-relics.js` | 遺物データ(`isRare:true`フラグ付きのレア遺物が56種中10種) |
| `data-enemies.js` | 敵・ボスデータ(画像込み) |
| `data-skins.js` | **新規** スキンデータ(SR21体+SSR9体=30体)。約7.2MB |
| `data-accessories.js` | **新規** アクセサリデータ(23種)。約0.75MB |
| `data-auras.js` | **新規** オーラのメタデータ(5種、見た目は未実装) |

### 音声・画像(リポジトリ直下)

- BGM: `bgm-menu.mp3` / `bgm-battle-normal.mp3` / `bgm-battle-elite.mp3` / `bgm-battle-elite2.mp3` / `bgm-boss-mid.mp3` / `bgm-boss-rush-1〜3.mp3` / `bgm-victory.mp3`
- BGM(タイトル配下の各画面): `bgm-select.mp3`(モンスター選択) / `bgm-gacha.mp3`(ガチャ) / `bgm-shop.mp3`(継承ショップ・カスタム装備)
  - **必ずmp3にすること。** m4a(AAC)は再生できないブラウザがある(実際にAAC非対応のChromiumで無音になった)。
    受け取った音源がm4aなら、PyAV(`pip install av`)でmp3に変換してから入れる
  - 追加したら `playSceneBGM()` / `stopSceneBGM()` の仕組みに乗せる。画面を閉じるときは `backToMenuBGM()`。
    `resumeCurrentBGM()`(復帰時・初回タップ時に呼ばれる)にも判定を足さないと、復帰後に無音になる
- SE: `se-select.wav`(選択音) / `se-damage.wav`(ダメージ音)
- 画像: `bg-dungeon.webp`(タイトル・マップ背景) / `title-logo.webp` + `title-logo.png`(ロゴ)

### 運用ルール

- **変更・追加があったファイルだけ**をアップロードすればOK
- `index.template.html`(単一ファイル版): データ4〜7ファイルを`index.html`に埋め込んだ動作確認用。GitHubへのアップロード不要。
  - **制約**: SEは`fetch()`で別ファイルを読む実装のため単一ファイルには埋め込めない(合成音にフォールバック。仕様上の制約でバグではない)
- 音声・画像を追加する時は、AudioContext/GainNode経由で音量調整できる仕組みに乗せる必要があるため、コード側の対応もセットで行う

---

## 3. 実装済みの機能(既存)

### サウンド

- BGMシステム一式(場面・階層・モードに応じて自動切り替え)
- SE差し替え(Web Audio APIの`AudioBuffer`で事前デコードし体感遅延をなくしている)
- 音量調整パネル(BGM音量・SE音量を別々に調整可能)
- **iOSは`<audio>`要素の`.volume`を無視する仕様のため、Web Audio APIのGainNode経由で音量制御している**(これが正解の実装方式)

#### 音が止まった状態からの復帰(触るときは必読)

- **`__currentBgm`** に「今の画面で鳴っているべきBGM要素」を1つだけ持たせている。
  BGMを鳴らす関数(`playMenuBGM`/`playBattleBGM`/`playVictoryBGM`/`playSceneBGM`)は必ずこれを更新し、
  止める関数(`stopMenuBGM`/`stopBattleBGM`/`stopVictoryBGM`/`stopSceneBGM`)は自分が止めた要素なら消す。
  **BGMを1系統増やすときは、この更新と消去もセットで書くこと。** 忘れると復帰やミュート解除で無音になる
- **`wakeAudio()`** が復帰の要。`visibilitychange`(表示に戻った時)・`focus`・`pageshow`・
  あらゆるユーザー操作(`pointerdown`/`touchstart`/`click`)から呼ばれ、
  AudioContextを起こし直し、止められていた`__currentBgm`を**頭出しせずに**鳴らし直す
- 他アプリへの切り替え・画面ロック・着信でAudioContextはいつでも`suspended`にされる。
  こうなると`<audio>`は`paused===false`のまま音だけ出なくなり、各play関数の
  「もう鳴っているなら何もしない」判定に引っかかって**永久に無音**になる。
  **だから起動用のリスナーを`{once:true}`にしてはいけない**(実際にこれで無音バグを出した)
- ミュート解除・復帰では`__currentBgm`をそのまま鳴らすので、曲が頭出しされたり
  ボスラッシュの曲が抽選し直されたりしない。画面から推測し直す処理(`resumeCurrentBGM`の後半)は
  `__currentBgm`が空のときだけのフォールバック
- **`bgmVolume`と`__currentBgm`はサウンド節の先頭(`soundEnabled`の隣)で宣言している。**
  `ensureAudioCtx()`/`wakeAudio()`が参照し、これらは「読み込み中に画面をタップされた」だけで
  呼ばれうるため、宣言をBGM節まで下げると`let`のTDZに当たって音が出なくなる
- **選択音の二重再生**: ボタンには共通のクリック音を鳴らす仕組み(capture段の`click`リスナー)があるので、
  `onclick`の中で個別に`playSfx('select')`も呼んでいる箇所は2回鳴ってしまう。
  `SFX_DEDUPE_MS`で`select`だけ60ms以内の重複を1回にまとめて対処している
  (戦闘SEは連打・多段ヒットで連続して鳴るのが正しいので対象外)
- **裏に回っている間はSEを鳴らさない**(`playSfx`/`playGachaSfx`の先頭で`document.hidden`を見る)。
  タブが裏だと`ctx.currentTime`が進まないので、その間に予約した音は全部「同じ時刻」に積み上がり、
  戻った瞬間にまとめて鳴る。タイマーも裏では間引かれるので溜まった処理が一気に走り、同じことが起きる。
  「戻ると変な音が繰り返し鳴る」と報告された症状の原因
- **`playBgmElement()`を通すこと。** `play()`は非同期なので、生の`el.play()`を連続で呼ぶと
  再生開始が重なって音が壊れる。`__bgmPlayPending`で1回にまとめている
- **`wakeAudio()`は200msで間引いている。** 復帰時は`visibilitychange`/`focus`/`pointerdown`/`click`が
  続けて飛んでくるため。ただし**間引くのは「BGMを鳴らし直す」部分だけ**で、
  `ensureAudioCtx()`(＝AudioContextのresume)は毎回通す。
  iOSは「ユーザー操作の中で呼ばれたresume」だけを通すことがあり、そこを間引くと復帰できない。
  ミュート解除(`resumeCurrentBGM`)は`wakeAudio`を通さず`playBgmElement`を直接呼ぶこと。
  通すと同じタップの間引きに巻き込まれて鳴らなくなる

##### 音の見張り番(`startAudioWatchdog` / `audioWatchdogTick`)

**iPhoneで電源ボタンを押して画面を消し、戻ると音が出なくなる**という報告への対処。
以前は「戻ってきた時に1回だけ復帰を試す」実装で、これが効かないケースがあった。

- **復帰処理を「1回試して終わり」にしてはいけない。** iOSの中断あけには次の性質がある:
  - 戻った直後(`visibilitychange`/`focus`の時点)の`resume()`/`play()`は**拒否されることがある**。
    ユーザーが画面を触るまで音を鳴らさせてくれない
  - `resume()`も`play()`もPromiseで失敗するので、**握りつぶすと失敗に気づけない**。
    旧コードは両方とも`.catch(()=>{})`で捨てており、1回失敗するとそのまま永久に無音だった
  - `<audio>`が`paused===false`の顔をしたまま`currentTime`が進まないことがある
- そこで**「音が戻ったと確認できるまで、400msごとに何度でも試す」見張り番**を置いている。
  最長20秒試し、直らなければ諦める(次のタップや復帰でまた動きだす)
- **「鳴っているか」はフラグではなく`currentTime`が実際に進んだかで判定する。**
  `paused`を見るだけでは詰まりを検出できない
- 「再生中なのに進まない」の検出は**2回連続で止まっていたときだけ**。1回で判定すると
  ループで曲が頭に戻った瞬間を誤検出して鳴らし直してしまう
- **止まっている`<audio>`に`currentTime`を書き込んではいけない**(`restartBgmAtCurrentPos`)。
  止められた直後の要素にシークをかけると位置を読み直せず**0(曲の頭)に戻る**。
  実際にこれで「復帰すると曲が頭出しされる」回帰を出した。
  止まっているなら位置はそのまま残っているので、何も触らずに`play()`するのが正しい。
  入れ直しが要るのは「再生中の顔をしているのに進まない」場合だけ
- **通常プレイ中は動かない。** タップのたびに起こすのは無駄なので、`audioLooksBroken()`で足切りし、
  復帰(`visibilitychange`/`focus`/`pageshow`)のときだけは見た目が正常でも必ず見張る
  (「鳴っているのに進んでいない」はそこでしか捕まえられないため)。
  正常だと確認できた時点で自分で`clearInterval`する
- ミュート中(`soundEnabled===false`)は起動しない。`toggleSound`のOFF側で`stopAudioWatchdog()`も呼ぶこと。
  忘れると自分で止めたBGMを見張り番が鳴らし直してしまう
- `__bgmGacha`は演出と同期した一発物なので対象外(`watchedBgm()`が除外する)
- 状態変数(`__audioWatchdogTimer`など)は**サウンド節の先頭で宣言している**。
  `ensureAudioCtx()`から見張り番に入ることがあり、宣言を下げるとTDZに当たる(`bgmVolume`と同じ理由)
- 検証: `scratchpad/audio_resume_verify.js`(中断復帰・resume拒否・詰まり・ループ誤爆・ミュート・
  ガチャ除外・回りっぱなし防止の8項目)
- **AudioContextは`state !== 'running'`で必ずresumeする。** iOSは中断時に非標準の
  `'interrupted'`になるので、`'suspended'`だけを見ていると復帰できない。
  併せて`statechange`でも起こし直している
- **マナーモード(消音スイッチ)対応**: iOSはWeb Audio API経由の音を消音スイッチに関係なく鳴らす。
  このゲームはSEも`<audio>`のBGMもAudioContext経由なので、放っておくと全部貫通する。
  `applyAmbientAudioSession()`が`navigator.audioSession.type = 'ambient'`を指定して回避している
  (Audio Session API / Safari 16.4以降。非対応環境ではtry-catchで無視)。
  **既定の`'auto'`はWeb Audioを使うと`'playback'`扱いになって消音スイッチを無視するので、必ず明示すること。**
  `'ambient'`は他アプリの音楽を止めない(ミックスされる)副次効果もあり、ゲームとしてはこちらが正しい。
  `ensureAudioCtx()`の先頭で毎回呼んでいる(AudioContextを作る前に指定する必要があるため)

### 最近の修正(このセッションで対応済み)

1. **戦闘勝利時にダメージSEが残る不具合**: `playSeBuffer()`で再生中のバッファを追跡し、`stopAllSe()`で`winBattle()`冒頭に強制停止
2. **ボス遺物選択画面に勝利BGMがない**: `showBossRelicChoice()`に`playVictoryBGM()`を追加
3. **モンスター選択の説明文とステータスのズレ**: `data-species.js`のdesc内「ライフ◯◯」を実際の値に修正(モッチー110 / ゴーレム100 / モノリス65 / カワズモー150 / ガリ75)
4. **手札が4枚しか見えない**: 手札の枚数に応じてカードサイズを自動縮小(`data-density`属性で`sm`/`xs`を切り替え)
5. **ダメージ予測が実際と違う**: `calcPreviewDmg()`を実際のダメージ計算式と完全に一致するよう全条件分岐を反映。敵の攻撃予告側も炎上/出血/氷結時の0.9倍系遺物を反映
6. **上部バーの整理**: 2段構成に変更(1段目=ステータス、2段目=遺物欄+🎴デッキ一覧+音量+ギブアップ)
7. **低い画面の端末対応**: `@media (max-height: 700px)`でコンパクトレイアウトに自動切り替え

---

## 4. 継承ショップ(メタ通貨システム) — 実装済み

序盤の理不尽さで離脱するのを防ぐための、周回で貯まる永続強化システム。

### 継承ポイントの入手

```
継承ポイント = 到達階数 × 3 × 難易度倍率
難易度倍率: ノーマル1.0 / ハード1.2 / エキスパート1.5 / ベリーハード1.75
+ ボス到達ボーナス: 15階+20 / 30階+40 / 45階+60 / 60階+100 / 63階+150
```

冒険終了時(全クリ/敗北/ギブアップの3パターン全て)に自動付与し、結果画面に表示。

### ショップの内容(タイトル画面「🏯 継承ショップ」から)

| 項目 | 効果 | 価格(段階ごと) |
|---|---|---|
| 開始ライフ+5 (×10段階) | 最大+50 | 40/55/75/100/130/165/205/250/300/360 |
| 開始チカラ+3 (×5段階) | 最大+15 | 60/110/180/270/380 |
| 開始丈夫さ+3 (×5段階) | 最大+15 | 60/110/180/270/380 |
| ガッツ回復量+1 (×5段階) | 最大+5 | 80/150/240/350/480 |
| ガッツ上限+4 (×5段階) | 最大+20 | 70/130/200/290/400 |
| 初期遺物解放 | 冒険開始時に遺物を1つ持てる | 1000(買い切り) |
| SSRカード確率アップ (×3段階) | SSR出現率 +0.5% / +1.0% / +1.5%(累計) | 500/1000/2000 |
| MRカード確率アップ (×3段階) | MR出現率 +0.25% / +0.5% / +1.0%(累計) | 750/1500/3000 |
| レア遺物確率アップ (×3段階) | isRare遺物の出現率 +2% / +4% / +7%(累計) | 700/1400/2500 |
| 初期ゴールド+15 (×10段階) | 最大+150 | 50/75/100/300/500/800/1100/1400/1700/2000 |
| 取得ゴールドアップ (×5段階) | 入手ゴールド +5%〜+25%(累計) | 500/1000/1500/2000/2500 |
| 初期手札+1 (×1段階) | 戦闘開始時の手札が1枚増える | 3000(買い切り) |
| 開始チカラ・極+1 (×10段階) | 最大+10。上の「開始チカラ」とは**別枠**で両方買える | 全段階10000 |
| 開始丈夫さ・極+1 (×10段階) | 最大+10。上の「開始丈夫さ」とは**別枠**で両方買える | 全段階10000 |
| 不屈 | 1冒険に1度だけ、ライフ0でも最大ライフの30%で復活 | 3500(買い切り) |

**「・極」の2枠は、全部買い切ってしまった人のための受け皿。** 1段階あたり+1しか伸びないかわりに
価格が一律で、上限も緩い。同じ趣旨の枠を足すときは `META_SHOP_ITEMS` に1行足し、
`loadMetaProgress()` の `fallback.levels` と `applyMetaShopBonuses()` の3箇所を揃えること
(fallbackを忘れると古いセーブで `undefined` になる)。

**取得ゴールドアップの注意**: ゴールドの加算は必ず`gainGold()`を通すこと。
`state.gold += n`と直接書くと倍率が乗らず、項目が効かなくなる。

**不屈の注意**: ヒノトリの種族特性の復活とは**別枠**なので、ヒノトリなら2回復活できる。
`gameOver()`の中で種族特性→不屈の順に判定している。

### 初期遺物解放(1000pt)— 実装済み

購入済みなら、**種族選択の直後に候補3つが提示され、1つ選んで持ち込める**(`showStarterRelicChoice()`)。
1000ptと高価な買い切りなので、ランダムで1つ渡すより選べた方が納得感があるという判断。

候補は`STARTER_RELIC_POOL_IDS`(12種)。**idだけを持ち、実データは`ALL_RELICS`から引く**ので
遺物の性能を調整してもこちらを直す必要はない(データ二重管理による不整合を避けるため)。

| 系統 | 候補 |
|---|---|
| 万能 | カニのはさみ(力+3 丈夫さ+5) |
| 攻め | パンチグローブ(力+3 初期ガッツ+5) / ネンドロシール(ライフ25 力4) |
| 守り | 鉄の盾(丈夫さ+3 初手ブロック10) / だんろ石(常にブロック3) / アルテミス像(毎ターンブロック2) |
| ガッツ | ガッツ豆(毎ターン+2) / ピクシーの羽(初期ガッツ+10) / ガッツミン(上限+10) |
| 耐久 | 白銀モモ(ライフ+25) / 双子の水差し(毎ターンライフ2回復) |
| 長期 | 秘伝の書(ランダムに1枚強化) |

**意図的に外しているもの**: レア遺物(`isRare`)とボス遺物(冒険中に見つける楽しみを残すため)、
種族専用(`mid`付き。種族ごとの当たり外れが出るため)、ゴールド系(序盤の生存に繋がらないため)、
黄金モモ(ライフ+50)やグジラシール(力6)など強すぎるもの(序盤が作業になるため)。

候補を増減したい時は`STARTER_RELIC_POOL_IDS`にidを足し引きするだけでよい。

### 保存

localStorageキー `mf_meta_progress`。既存のセーブ・ランキングとは独立。

---

## 5. ダイヤ通貨システム — 実装済み

ガチャ専用のレア通貨。localStorageキー `mf_dia_progress`。

| 入手方法 | 内容 |
|---|---|
| ログインボーナス | 毎日15、5日連続ごとに+50 |
| **初回限定ウェルカムボーナス** | 初めて起動した時のみ+300(`welcomeBonusClaimed`フラグで管理) |
| 累計プレイ回数 | 1/10/30/50回 → 100/100/150/200、以降30回ごとに250(無期限) |
| 階層到達(毎回) | 15階 3/4/5/7・30階 5/7/9/12・45階 8/11/14/18・60階 15/20/26/35(難易度別) |
| 63階(ベリーハードのみ) | +50 |
| 種族別初踏破称号(1回のみ) | 60階以上到達で種族×難易度ごとに1回 50/70/90/120(63階なら150) |

タイトル画面の右上に固定バッジで所持数を表示。

### 称号画面(タイトル画面「🎖️ 称号・挑戦回数」)

回数報酬のダイヤが何の説明もなく増えて分かりにくかったため追加した画面(`window.game.showTitles()`)。

- **通算の挑戦回数**と、次の回数報酬が何回目でいくら貰えるか
- **回数報酬の一覧**(達成済み/未達成。`listDiaPlayMilestones()`が直近4件+次の2件を返す)
- **種族別の初踏破称号**(7種族 × 4難易度 = 28個)。難易度ごとに格が変わる
  (ノーマル=踏破者 / ハード=猛者 / エキスパート=達人 / ベリーハード=覇者)

あわせて、**回数報酬を貰った瞬間にトーストで通知**するようにした(節目でない回は通知しない)。
`registerDiaPlayCount()`は以前から獲得量を返していたが、呼び出し側が捨てていたため無言で増えていた。

---

## 6. ガチャ — ロジック実装済み・演出は未実装

localStorageキー `mf_gacha_progress`。タイトル画面「💎 ガチャ」から。

### コスト

単発 💎15 / 10連 💎135(1割引)

### 排出率

`GACHA_RATES`がそのまま正。表示(`showGachaRates()`)もここから組み立てるので、値を変えれば表示も追従する。

| 枠 | 確率 | もらえるもの |
|---|---|---|
| ハズレ | 53% | 継承ポイント20 |
| レア・大量継承pt | 10% | 継承ポイント150 |
| レア・アクセサリ | 15% | アクセサリ1個 |
| SRスキン枠 | 12% | SRスキン または モーション(半々) |
| SSRスキン | 5% | スキン(SSR)。うち3.5%はピックアップ(下記) |
| ウルトラレア・オーラ | 5% | オーラ1種 |

### ピックアップ(確率アップ)

`GACHA_PICKUP.skins`に載せたSSRスキンは、**SSR枠5%のうち3.5%**を占める(残り1.5%がそれ以外のSSR)。

- 実装は`rollSsrSkin()`の1か所だけ。`GACHA_PICKUP_SSR_RATE / GACHA_RATES.ssrSkin`を
  「SSRを引いたときにピックアップになる確率」として使う
- **ピックアップを載せ替えるときは`GACHA_PICKUP.skins`を書き換えるだけでよい。**
  `GACHA_PICKUP.date`も一緒に更新する。`accessories`は空配列でよい(その欄ごと出なくなる)。
  バナー・提供割合の表示・抽選の3か所が全部そこを見ている
- ピックアップが空、または「それ以外」が空になったら、普通に全体から引く動きに戻る
  (バナーを畳んだときに壊れないようにしてある)
- **`GACHA_RATES.ssrSkin`を変えるときは`GACHA_PICKUP_SSR_RATE`との関係(3.5 ≦ 5)も見直すこと**

### 天井

- 10連に1回、レア(アクセサリ)以上確定
- 累計100連ごとに、SRスキンかモーションを選んで入手
- 累計200連ごとに、SSRスキンかオーラを選んで入手
- 選択画面(`showPityChooser()`)では、**持っているものに「入手済み」、持っていないものに🆕**が付く。
  所持判定はスキン/オーラ/モーションで見る場所が違うので、種別を増やしたら`isOwned`にも足すこと
- **注意**: SR天井とSSR/オーラ天井は独立カウント。200連目・400連目は**両方同時に発火**する(実装済み・検証済み)

### 被り(重複)時の還元

| 種別 | 継承ポイント |
|---|---|
| SRスキン | 100 |
| SSRスキン | 400 |
| アクセサリ | 50 |
| オーラ | 400 |

### 抽選範囲

スキンは**全7種族×全スキンから完全ランダム**(自分が育てていない種族のスキンも出る)。

提供割合の画面(`showGachaRates()`)の「全◯種」は`allSkinsOfRarity()`や`ACCESSORIES.length`から
数えているので、スキンやアクセを増やしても表示は勝手に追従する(以前は手書きの数字でズレる元だった)。

---

## 6.5 難易度と敵の強さ

難易度ごとの倍率は`DIFF_ENEMY_MULT`(通常敵・強敵)と`DIFF_BOSS_MULT`(ボス)の2つの表で持つ。
`diffMultFor(isBoss)`がどちらを使うか決める。

| 難易度 | 通常敵・強敵 HP | 通常敵・強敵 攻撃 | ボス HP | ボス 攻撃 | スコア |
|---|---|---|---|---|---|
| ノーマル | 1.00 | 1.00 | 1.00 | 1.00 | 1.0 |
| ハード | 1.05 | 1.12 | 1.00 | 1.20 | 1.2 |
| エキスパート | 1.10 | 1.20 | 1.20 | 1.25 | 1.5 |
| ベリーハード | 1.15 | 1.28 | 1.30 | 1.30 | 1.75 |
| レジェンド | 1.40 | 1.40 | 1.50 | 1.50 | 2.25 |

### 過去にあった不具合(2026/08/04に修正)

**難易度を上げても通常敵と強敵の攻撃力が1ダメージも上がっていなかった。**
ハードに至ってはHPすら増えず、通常戦闘がノーマルと完全に同一だった。

原因は、敵の実ダメージが**技ごとの固定値**(`e.intent.val`)から作られていたのに、
難易度補正は`startBattle`で`t.dmg`にかけていたこと。`t.dmg`は
「戦闘中に敵が力アップした差分」(`state.enemy.dmg - state.enemy._baseDmg`)を
測るためだけに使われており、被ダメージには一切影響しない。
ボスと一部の特殊敵だけが分岐の中で`adjust()`を通していたため、
ボスだけ正しくスケールしていた。

### かけ忘れが起きない構造にしてある

- `setEnemyIntentBase()` … 敵ごとの分岐。**素の数値をそのまま入れる**(倍率をかけない)
- `setEnemyIntent()` … 上を呼んだあと、`intent.val`に難易度倍率を一括でかける

敵を追加するときは`setEnemyIntentBase()`に分岐を足すだけでよく、
倍率のことを気にしなくてよい。**分岐の中で倍率をかけないこと**(二重にかかる)。

攻撃予告の表示も`intent.val`をそのまま出しているので、表示と実ダメージは自動的に一致する
(レジェンド35階グジラで、予告68・実ダメージ68を確認済み)。

---

## 6.54 冒険の自動復帰(タスクキル対策)

`mf_active_run` に `state` ごと保存し、起動時に `tryResumeRun()` で戻す。
画像(`img`)はlocalStorageの容量を食うので保存せず、復帰時に
`SPECIES[id]` と `relinkEnemyImg()` で貼り直す。

### **`tryResumeRun()` はスクリプト全体の評価後に呼ぶこと**

```js
if(document.readyState === 'loading') document.addEventListener('DOMContentLoaded', tryResumeRun);
else tryResumeRun();
```

以前はこの関数がファイル中ほど(2100行目付近)で**即時実行**されていた。
`renderBattleSprites()` → `getEquippedSkinImg()` → `loadCosmetics()` の順に呼ばれ、
その中で `ACC_SLOT_KEYS`(2900行目)や `CARD_MOTIONS`(6900行目)といった
**まだ評価されていない`const`**に触るため、TDZの`ReferenceError`になっていた。
例外は`catch`に飲まれるので画面にエラーは出ず、
**復帰処理だけが途中で止まってキャラも敵も表示されない**という形で現れた。

**`typeof X === 'undefined'` のガードはTDZのconstには効かない**(それ自体が投げる)ので、
「安全のためtypeofで囲む」は対策にならない。実行順を直すのが正解。

### 敵の行動(intent)が無い状態での復帰

保存のタイミングによっては`state.enemy.intent`が無いまま復帰することがある。
`updateUI()`は`intent.type`を読むので、以前はここで例外が出てUI更新が止まっていた。
今は`updateUI()`側でフォールバックし、`tryResumeRun()`でも`setEnemyIntent()`を呼び直している。

### 山札・捨て札・消滅の一覧(`showPileModal()`)

バトル画面のガッツの左右にある `Deck` / `Grave` をタップすると中身を見られる
(`window.game.showDrawPile()` / `showDiscardPile()`)。

- 3つ(山札 / 捨て札 / 消滅)をタブで切り替える1つの画面(`showPileModal`/`PILE_TABS`)。
  バトル画面にボタンを増やすと狭くなるのと、「捨て札のついでに消滅も見たい」が1タップで済むため
- **山札は必ず名前順に並べ替えて出すこと**(`PILE_TABS`の`hideOrder`)。
  `state.drawPile`の順番のまま並べると「次に何を引くか」が丸見えになり、
  引きの駆け引きが成立しなくなる。捨て札は既に使ったカードなので新しい順でよい
- 消滅したカードは`state.exhaustPile`。「消滅」タブで見られる

### **キャラの絵は「描き直す場所」を増やしてはいけない — `renderPlayerSprite()` に集約**

自キャラの絵・種族名・アクセサリ・オーラは`renderPlayerSprite()`が一手に描く。
呼ぶのは3か所:

1. `selectMonster()` — 冒険の開始時
2. `startBattle()` — 戦闘に入るたび
3. `tryResumeRun()` — 復帰したとき(**中断した場面に関係なく必ず**)

**`startBattle()`は敵の絵しか描かない**、というのが罠だった。
普通に遊ぶ分にはキャラの絵は開始時に1度描けばDOMに残り続けるので問題にならないが、
途中でタスクキルするとDOMは真っ新から始まる。
以前は復帰処理が`payload.scene === 'battle' && !state.battleEnded`のときしか
`renderBattleSprites()`を呼んでいなかったため、
**マップ・イベント・ショップ・報酬選択の途中で中断した場合はキャラが一度も描かれず、
種族名が「---」のまま、キャラが見えない状態で次の戦闘が始まっていた**(実際に報告された)。

新しく「キャラを表示する画面」を足すときは、自前で`playerVisual.innerHTML`を書かず
`renderPlayerSprite()`を呼ぶこと。描画箇所が増えるとまた同じ穴が空く。

---

## 6.545 敵の強さを触るときの注意

### **`data-enemies.js` の `dmg` は実ダメージではない**

被ダメージを変えたくて `dmg` を上げても**1も変わらない**。`dmg` は「戦闘中の力アップ分」の基準値
(`_baseDmg` との差分)でしかない。実ダメージはこちら:

| 相手 | 実ダメージの出どころ |
|---|---|
| 通常敵 | `setEnemyIntentBase()` の trait ごとの `val` |
| 強敵 | `data-enemies.js` の `actions[].dmg` |
| ボス | `setEnemyIntentBase()` の trait ごとの `val` |

いずれも最終的に `intent.val` になるので、**まとめて倍率をかけるなら `setEnemyIntent()`** が唯一の場所。

### 創造神テクモクレイン(65階・3形態) — **公開済み**

`index.html` の **`CREATOR_BOSS_ENABLED`** が `true` で、レジェンドボスラッシュは
**64階=休息所 / 65階=ラスボス** まで伸びている(`false` に戻すと63階終わりに戻る)。
**確認は管理者ページの「🌌 創造神と戦う」から**(ノーマル/レジェンドを選んで即開始できる)。

- **64階は`initMap()`で特別扱い**にしてある(休息所・修行・ショップの3つから選ぶ)。
  他の休息階(`restFloors`)は2つからだが、ラスボス直前だけ3つ全部出す
- 殿堂入りの文言は`CREATOR_BOSS_ENABLED`から階数を出しているので、切り替えれば自動で追従する
- `nextFloor()`の`maxFloor`も同じフラグを見ている。**この3か所はフラグ1つで揃う**

- HPが0になるたび復活して形態が変わる(`revive:true, reviveMax:2` で3形態)。
  **HPは1形態ぶんの数値**(素4000 / レジェンド6000 → 実質18000)
- 形態は `e._reviveCount` で見る。0=秩序 / 1=混沌 / 2=創造
- `formNames` を持つ敵は、名前が「創造神テクモクレイン・混沌」のように変わる(`onEnemyFormChange`)。
  **第1形態ぶんも `startBattle` で `onEnemyFormChange(e, 0)` を呼んで付けている**
  (呼ばないと1形態目だけ呼び名が違ってしまう)
- **形態が変わるとカット・反射・ブロックを消し、`turnCount` を0に戻す。**
  戻さないと必殺技と「隙」の周期が形態をまたいでズレる

#### 見た目(大きさ・名前)

- **ラスボスだけ絵を2.4倍で出す**(`ENEMY_SPRITE_SCALE.creator`)。
  等倍だと自キャラと同じ大きさで、ラスボスに見えなかった
- **拡大の基準は上端**(`ENEMY_SPRITE_ANCHOR_TOP`)。中央基準だと頭が上部バーに隠れる。
  足元は下のUI(Grave/Deckの数字)に重なるが、そのほうがラスボスらしい。
  重なった先のタップを邪魔しないよう `pointer-events:none` にしてある
- **2.4倍が上限のめやす。** 2.8倍にすると絵が画面の右にはみ出す(検証で確認)
- **絵を上へ持ち上げる**(`#enemy-visual.sprite-lifted` / `--elift: -190px`)。
  敵の枠は「攻撃予告 → HPパネル → 絵」の縦並びなので、大きくしただけだと絵が下に押し出され、
  上半分が空いて「ボスが下にいる」見た目になる。
  - **⚠️ margin ではなく `position:relative` + `top` で動かすこと。**
    敵の枠は下端そろえなので、marginで縮めても「絵が上がる」のではなく「パネルが下がる」だけになる
  - **⚠️ `transform` も使わないこと。** `.monster-sprite` が idle アニメで transform を使っている
- **持ち上げたら、攻撃予告とHPパネルは下へ逃がす**
  (`#enemy-monster-node.boss-hud` / `--ehud: 96px`)。
  絵だけ上げると、この2つがちょうどボスの顔を覆ってしまう。ずらした先はボスの裾のあたり。
  HPパネルには `relative z-20` が要る(絵の `z-index:10` に負けて隠れるため)
- いまの数値での位置関係(390x844): 絵 186〜455 / 予告 375〜405 / HP 411〜470 /
  上部バー下端 89 / 残り枚数バッジ 566 / 手札 592。**どこも重なっていない**。
  数値を変えたら `scratchpad/hud_measure.js` で重なりを測り直すこと
- 敵の名前は `truncate w-32` をやめて折り返すようにした。
  固定幅で切ると「創造神テクモク…」になり、長い名前のボスで名乗りが読めない

#### 形態ごとの絵と、形態変化の演出

- 絵は `data-enemies.js` の **`formImgs`(3枚)**。`img` には第1形態ぶんと同じものを入れてある
  (形態を持たない敵と同じ扱いで描けるようにするため)。420px高のPNG、256色に減色して各90〜120KB
- 差し替えとリセットは `onEnemyFormChange` → `playFormChangeFx` が行う。3段構成:
  1. **溜め**(0〜550ms) … 絵が明るくなり、画面が小刻みに震え、外から光の粒が吸い込まれる
  2. **爆発**(550ms〜) … 絵が白く飛んで消え、輪4つ・光の筋20本・破片18個が広がる。看板と画面揺れも同時
  3. **顕現**(850ms〜) … 新しい絵が上から降りてきて、足元で魔法陣が回りながら開く
- 絵の入れ替えは爆発の300ms後。**次の絵は先に `new Image()` で読み込んでおく**(入れ替えた瞬間の空白を防ぐ)
- 色は `FORM_CHANGE_THEME`(金→紫→桃紫)。看板の上の小さい文字も同時に変わる
- **⚠️ 全画面の光(`fchg-flash`/`fchg-white`)を濃く長くしないこと。**
  一度 flash を最大1.0・white を0.92で出したところ、輪も破片も真っ白に埋もれて何も見えなくなった。
  いまは flash 0.72 / white 0.6 で、どちらも一瞬だけ
- **⚠️ 演出の中心は `ui.enemyVisual`(絵)に合わせること。**
  `ui.enemyNode` の中心にすると攻撃予告とHPバーのぶん上にズレて、ボスの頭上で爆発しているように見える
- **⚠️ 演出を減らす設定(`html.fx-off` / `prefers-reduced-motion`)のときは、animationを止めるだけにすること。**
  `display:none` で消すと絵が第1形態のまま戦うことになる。絵の差し替えと名前の更新は必ず走らせる
- **名前の表示は `onEnemyFormChange` の中で直接書き換えている。**
  呼び出し側の `updateUI()` 待ちにすると、絵と看板は新形態なのに画面の名前だけ前の形態、という一瞬が出る
- **`relinkEnemyImg` は `formImgs` を見て「いまの形態」の絵に戻す。**
  中断→再開したときに第1形態の絵で戦わないようにするため

#### この敵のために足した効果キー(intentに書く)

| キー | 効果 |
|---|---|
| `nextHandMinus` | 次のターンの手札を減らす(既存の`nextTurnHandReduce`に乗せている) |
| `dmgCutPct` / `dmgCutTurns` | 敵が受けるダメージを%カット。ターン開始時に1ずつ減る |
| `thorns` / `thornsTurns` | 攻撃を受けるたび反射。**連撃は1発ごとに跳ね返る**(連撃4なら4回) |
| `clearCut` / `clearThorns` | 「隙」の技が自分でカット・反射を解除する |
| `openWindow` | 「隙」。ブロック0にして守弱1を付ける(＝こちらの与ダメージ1.5倍) |
| `bigInjury` | `injuryCards`で入れるのを大ケガにする(難易度に関わらず) |

#### パッシブ「🌀 理を無視する」(`defPierce`)

**プレイヤーの丈夫さを一定割合だけ無視して攻撃する**(創造神テクモクレインは0.25)。

これを入れた理由は、**丈夫さが「1発ごとに」引かれる**ため。65階では丈夫さ90超が普通で、
そのままだと**連撃技ほど無力になる**。さらに攻弱(×0.75)は丈夫さを引く**前**にかかるので、
攻弱を撒くだけで技が消える状態だった。

実測(丈夫さ92・レジェンド・貫通なしのとき):

| 技 | 素 | 攻弱あり |
|---|---|---|
| 星々の慟哭(連撃3) | 75 | **0** |
| 歪みの奔流(連撃2) | 2 | **0** |
| 世界を歪める | 13 | **0** |

**攻弱を撒くだけで5つの技が0ダメージ**になっていた(第3形態の平均 93→40)。
最初は貫通35%を入れて0になる技を無くした(丈夫さ92のとき実効59)。
その後**25%に下げた**(実効69)。「伝説のきまぐれ」の単発ぶんを下げてこちらの火力が落ちたぶん、
受けるダメージも合わせて緩めるため。

- ダメージ計算の順番は **攻弱(×0.75) → 守弱(×1.5) → 丈夫さを引く**。
  倍率が先なので、**守弱は1.5倍以上の働きをする**(第3形態の平均 125→235)。
  ボスは守弱を撒く技を4つ持っていて、ここは設計として効いている
- **⚠️ `effectivePlayerDef()` を必ず経由すること。** 実処理(敵の攻撃ループ)と
  予告(`updateUI`)の両方で使う。片方だけだと予告と実ダメージがズレる
- 割合を変えるなら `data-enemies.js` の `defPierce` だけ。
  検証は `scratchpad/pierce_verify.js`(予告と実ダメージの一致まで見る) と
  `scratchpad/status_impact.js`(攻弱・守弱こみの技別ダメージ表)

#### パッシブ「🌀 伝説のきまぐれ」の倍率(`whimSingleMult` / `whimMultiMult`)

61階以降のボスに共通で付く「連撃は半減、単発は増える」。**両方の倍率を敵ごとに変えられる**。
既定は 連撃`×0.5` / 単発`×2`。**創造神テクモクレインだけ単発`×1.3`**(連撃の半減は共通のまま)。

- 単発`×2`のままだと一撃で溶けてしまうので、ラスボスでは褒美のほうだけ控えめにしてある。
  単発と連撃の差が **4倍 → 2.6倍** に縮まる
- 実測(力90・ゴーレム): 単発カードの与ダメージが **-35%**。連撃は変わらない
  (渾身掌打437→284。ちなみに超竜巻アタック(連撃4)も284で、ちょうど並ぶ)
- `whimMultiMult` を使っている敵は**いまのところ無い**が、連撃側だけ緩めたくなったとき用に残してある
  (一度「連撃の半減をやめる」まで実装して戻した経緯があるため)
- **⚠️ `whimSingleMult()` / `whimMultiMult()` を必ず経由すること。**
  `playCard` と `calcPreviewDmg` の両方にある。片方だけだとドラッグ中の予想ダメージと実際がズレる
- **⚠️ `whimMultiMult` は 0 も 1 も意味のある値なので、`||` で既定値に落とさないこと**
  (`!== undefined` で判定している)
- 説明文(`showBossEffectInfo`)も両方の値から文章を組み立てている。
  連撃に罰が無い敵では「連撃はそのままだが」に言い回しが変わる
- 変えるなら `data-enemies.js` の2つだけ。検証は `scratchpad/whim_verify.js`
- **単発中心のデッキでは撃破までのターンが1.5倍近くになる。** 長すぎると感じたら、
  倍率を1.5に戻すか `hp` を下げるのが素直(HPを上げる調整は「ダレる」ので避けてきた経緯がある)

#### 形態ごとの撃破スコア(`formScores`)

`[300000, 600000, 1000000]`。第1形態を倒すと30万、第2形態で60万、第3形態で100万(合計190万)。

- 第1・第2形態ぶんは`tryEnemyRevive()`の中で、最後の形態ぶんは`winBattle()`で加算する
- **⚠️ `formScores`を持つ敵は、61階以降の一律ボーナス(`500000 + (階-61)*100000`)から除外してある。**
  外さないと65階で+90万が二重に乗る

#### パッシブ「⚖️ 創造の律」(`cardPlayLimit`)

**1ターンに使えるカードの枚数制限**。`legendaryAura`/`legendaryWhim` と同じボス固有パッシブ枠で、
✨アイコンをタップすると説明が読める。手札を無限に回す戦法を止めるためのもの。
**枚数は敵データの `cardPlayLimit`**(創造神テクモクレインは7枚)。
`CARD_LIMIT_DEFAULT` は敵が枚数を指定しなかったときの既定値。**両方そろえて直すこと**。

- 残り枚数を `#card-limit-badge` に出す。**理由が分からず使えなくなる事故を防ぐため必須**
- **`clickCard` と `playCard` の両方で見ること。** ドラッグで出す経路やカードから
  カードを使う効果は `clickCard` を通らない
- ターン開始時と戦闘開始時に `resetCardPlayLimit()` で戻す

### **敵の復活は `tryEnemyRevive()` に集約してある**

以前は復活の判定が**5箇所**(炎上/出血/感電/カード使用/例外復旧)にコピペされていて、
`_revived` という真偽値のため**1回しか復活できなかった**。回数管理に作り替えて1関数にまとめた。

- **新しく「敵のHPが0になる経路」を足したら、必ず `tryEnemyRevive()` を通すこと**
- `reviveMax` を省略すると1回(＝フェニックス等は今までと同じ挙動)
- 戻り値 `true` = 復活した(戦闘続行) / `false` = 倒した(勝利処理へ)

### **プレイヤーの弱体を書き込む先を間違えないこと(`atkBase` と `atkBattle`)**

プレイヤーの力・丈夫さは**2本立て**になっている。混同すると取り返しがつかない。

| 変数 | 中身 | いつ消えるか |
|---|---|---|
| `atkBase` / `blockBase` | 遺物・レベルアップ・ショップ・イベントで**手に入れた永続ぶん** | 冒険が終わるまで消えない |
| `atkBattle` / `blockBattle` | カード等で**その戦闘のあいだだけ**上げ下げしたぶん | 戦闘開始時に0へ戻る |

実際に使う値はどこでも**この2つの合計**(`atkBase + atkBattle`)。

- **敵の弱体(`atkDown`/`blockDown`)は必ず `atkBattle`/`blockBattle` を削ること。**
  以前 `atkBase` を削っていたため、ボスの「力-」「丈夫さ-」を受けると戦闘後も戻らず、
  46〜63階のボスラッシュを通すと**力-48/丈-52ほど回復不能に削られていた**(2026/08/06に修正)。
  攻弱・守弱・出血と同じく、敵の弱体はその戦闘限りが正しい
- **合計が負にならないよう必ず抑えること。** 防御力は `dmg = Math.max(0, dmg - def)` で引くので、
  `def`が負だと**引き算が加算になって被ダメージが増える**。`statDown()`が合計0で止めている
- 永続ぶんを減らしていいのは、プレイヤーが選んだ結果(イベントの選択肢・遺物のデメリット)だけ

### **連撃(`hits`)技の `val` は「1発あたり」**

`hits`を持つ技は、`val`のダメージを`hits`回ぶん叩き込む(1回ずつ防御を引く)。
**合計ダメージは `val × hits`** なので、`val`を少し触るだけで体感が大きく変わる。
攻撃予告に出る数字も1発ぶんで、技名の横に`(連撃×N)`と出る。

連撃を持つ敵の技は**イブリース(63階・trait `ark`)の3つだけ**:
「天の慈悲よ示されよ」(×4)「翔べ震律の刃よ」(×3)「熾天の剣よ降り立て」(×5)。
合計が跳ね上がって強すぎたため、2026/08/06に3技とも`val`を40下げた。

### 30階以降の強敵の底上げ

`ELITE_LATE_HP_MULT`(1.35) と `ELITE_LATE_DMG_MULT`(1.3)。判定は `isEliteBoosted()`。
素の状態では強敵と通常敵の差が HP+15% / ダメージ+11% しかなかった。調整後は
HP1.5倍前後 / ダメージ1.7倍前後(実測)。

- **`revive` を持つ敵はHP倍率の対象外。** 表示HPが低いのは意図的な設計で、一度全回復するぶん
  実質の耐久はすでに2倍ある(ヒノトリ 560 → 実質1120。素のデュラハン870より硬い)。
  ここに倍率をかけると実質1500超えになり、他の強敵を大きく追い越してしまう。
  ダメージ倍率のほうは対象のまま(火力は他の強敵より低いため)

- **判定に `e.isElite` を使ってはいけない。** あれは `mode!=='normal'` なので**ボスもtrueになる**。
  必ず `mode === 'elite'` で見ること(ボスは階ごとに手で調整した数値なので巻き込むと壊れる)
- ナリキロッグ(レアモンスター)は階層帯ごとに個別調整済みなので対象外
- 難易度倍率とは掛け算になる(レジェンドの強敵 = 素 × 1.35 × 1.4)

### 階層と出現マスの対応(調べた結果)

| 階 | 出るもの |
|---|---|
| 31〜44 | 通常敵・強敵あり(通常敵プールは `normal_late` の4種で固定) |
| 30 / 45 | ボス |
| 46〜59 | **休息所+ショップ/修行 と ボス の交互。通常敵も強敵も出ない** |
| 60〜63 | ボス |

**「30〜60階の敵をスケーリングする」という話は成立しない**(46階以降に道中の敵がいないため)。
道中の調整をするなら対象は 31〜44階だけ。

---

## 6.546 手札消滅カード(選別・昇華・整理整頓・忘却の礎)

戦闘中に手札のカードを選んで消滅させる汎用カード4枚。すべて`remove:true`(使ったら消滅)。

| カード | レア | コスト | キー |
|---|---|---|---|
| 選別 | R | 8 | `chooseExhaust:1, draw:1` |
| 昇華 | SR | 12 | `chooseExhaust:1, energyFromExhausted:true` |
| 整理整頓 | SR | 15 | `exhaustHandAll:true, drawPerExhausted:true` |
| 忘却の礎 | SSR | 8 | `chooseExhaust:2, draw:1, heal:30` |

### **カードを消費するのは「選び終わってから」**

選択モードの入口は`clickCard()`。`chooseExhaust`を持つカードをタップすると、
**まだ何も消費せずに**`beginExhaustPick()`で選択モードへ入り、選び終わってから`playCard()`を呼ぶ。

**先にカードを消費して選択待ちにしてはいけない。** その途中でアプリを閉じられると
「カードだけ消えて何も起きていない」状態で復帰してしまう。この順序なら、
途中でやめても落ちても何も起きていない状態に戻るだけなので壊れようがない
(選択中であることはセーブしないので、復帰すれば自動的にキャンセル扱いになる)。

### その他の注意

- 選択中は`renderHand()`が`onclick`を上書きする。**createCardUIは「使える札」にしかonclickを付けない**ので、
  ガッツが足りない札も選べるようにするにはこの上書きが必要
- 上へドラッグして出す操作は選択中は無効(`startDrag`で弾いてタップ=選択に一本化)
- ターン終了(`handleEndTurn`)と戦闘開始時に選択を解除している
- **呪い(`noRemove`)もその戦闘の間は消せる**。デッキからは消えないので、レジェンドの主旨は保たれる
- 手札に自分しかいないときは選択を飛ばしてそのまま使う(ドローや回復だけ発生)

---

## 6.547 カードの説明文を作り直す場所(`upgradeCard`)

強化すると`parts`の並びから説明文を組み立て直す。**効果キーを足したらここにも足すこと。**
書き忘れると、強化した瞬間にその効果の説明だけが消える(効果自体は働くので気付きにくい)。

以前は27枚が該当していた(「一撃入魂」→『消滅』だけ、「仕切り直し」→『7ドロー』だけ等)。
2026/08/05に全キーぶんの文言を追加して解消済み。

- **効果キーを持たず、カード名で分岐している効果もある**(「いじわる」「モン刺し」「ファイアウェーブ」)。
  これらも名前で判定して`parts`に足してある
- **「神核」「炎王」のように、名前だけでは何が起きるのか分からない効果は、中身まで書くこと。**
  以前は『この戦闘中「神核」を付与』で止めていたため、強化した瞬間に肝心の説明が消えていた
  (2026/08/06に修正)。名前を付けた新しい仕組みを足すときは同じ轍を踏まないこと
- 残り3枚(「万能薬」「血踊」「溶岩の型」)は強化すると説明が**短くなる**が効果は書かれている。意図的にこの粒度

---

## 6.5495 スキン専用の技セット(`skinSetOf`)

SSRスキンに「そのスキンで戦うときだけカードが差し替わる」技セットを持たせる仕組み。
初期技(`skinCardOf`)が**初期デッキの看板技1枚だけ**なのに対し、こちらは
**報酬・ショップ・イベントも含めた全ての入手経路**で入れ替わる。

実装済み: ゴジョモッチー(`motchi_ssr_02`) 5枚、ゲコウスグル(`kawazumo_ssr_02`) 5枚。
どちらも呪術廻戦のコラボ(五条悟／夏油傑)なので、**手触りが対になるように作ってある**。
ゴジョモッチーは「守弱を撒いて赫・茈で即爆発」、ゲコウスグルは「呪霊を溜めてうずまきで放つ」。

### 作り方(次のスキンに足すとき)

`data-cards.js` に `skinSetOf`(どのスキンのセットか)と `replaces`(元カードのキー)を持つカードを足すだけ。
装備画面・差し替え・バッジ表示が全部そこを見るので、**index.html を触る必要はない**。

- **レア度は元カードと必ず揃える。** ズレると報酬の抽選枠が変わって出現率が狂う
- `motion` にモーションidを書くと、そのカードだけ演出を固定できる

### 触るときの注意(全部つまずいた箇所)

- **`data-cards.js` のカードは大半が `id` を持たない**(`m_r9:{mid:'motchi',name:'お餅体制',...}`)。
  差し替え表はオブジェクトのキーで引くので、`c.id` だけを見ると**差し替えが一切効かない**。
  `cardKeyOf()` がキーを逆引きする。新しくカードを引く処理を書くときもこれを通すこと
- **プールからは専用カードを先に取り除いてから置き換える**(`skinSetPool`)。取り除かないと
  ①セットを使っていない人のプールに専用カードが混ざる
  ②使っている人は「置き換えたぶん」と「専用カードそのもの」が二重に並び**出現率が2倍**になる
- **カードを作る場所は必ず `mkDeckCard()` を通す。** 直接 `{...c, instanceId:...}` と書くと
  そこだけ元カードのまま入る。プールを作る場所は `skinSetPool()` を通す(現在14箇所+10プール)
- **使うかどうかは `selectMonster()` で `state.skinCardSet` に焼き付け、冒険中は変えない。**
  stateに持たせてあるのでセーブに載り、中断復帰後に手に入れるカードもきちんと専用版になる。
  途中で切り替わると「大モッチ砲」と「術式反転「赫」」が同じデッキに並ぶ
- **看板技は `applyStarterCardChoice()` がセットのものに強制する。** 装備画面の操作に頼らないので、
  どう選ばれていても食い違わない
- 「何体目のスキンか」は**プレイヤーに見せない**。技セットの有無をスキンの属性として
  バッジで示す(`hasCardSet()`)。あとからどのスキンに足しても破綻しない
- 検証: `scratchpad/cardset_verify.js`(全入手経路・二重出現・中断復帰・強化後の説明)、
  `scratchpad/cardset_play.js`(実戦での数値・連鎖・演出・装備画面)、
  `scratchpad/geto_verify.js`(ゲコウスグル。呪霊の加算・消費・表示と実際の一致・持ち越さないこと)

### 「呪霊」(ゲコウスグル専用のカウンター)

戦闘中だけ溜まる `state.player.curse`。`startBattle` で0に戻すので冒険をまたがない。

| カード側の項目 | 意味 |
|---|---|
| `curse:N` | 呪霊を N 溜める |
| `curseDmg:N` | 追加ダメージ 呪霊×N（消費しない） |
| `curseSpend:true` | 撃ったあと呪霊を0に戻す |
| `regenCurse:N` | 毎ターン開始時に呪霊+N |

- **`curseDmg` は `playCard` と `calcPreviewDmg` の両方に同じ式を書くこと。**
  片方だけだと「ドラッグ中に出る予想ダメージ」と実際がズレる(このリポジトリで何度も踏んでいる型)
- 足す場所は**カードの攻・力と同じ `b`**。つまり**守弱1.5倍・攻弱0.75倍・会心がそのまま乗る**。
  `comboDmg`(茈の守弱時+150)と同じ扱い
- **`curse` を溜めるのは攻撃処理より後**。だから同じカードの `curseDmg` は自分では強化しない。
  `curseSpend` は攻撃ループを抜けた直後・`c.curse` より前に置く(逆にすると溜めたぶんまで消える)
- 呪霊を溜めるだけのカード(盤星教の教祖)は攻撃も防御もしないので、
  `playCardMotion('buf')` の条件に `(c.curse||c.regenCurse) && !c.val && !c.block` を足してある
- バッジは `ps(...,'curse')`。`STATUS_DESCS.curse` に解説があるのでタップで読める

### 数値を決めるときの落とし穴

**守弱は「被ダメージ1.5倍」の効果も持つ**(`if(state.enemy.vuln>0)m*=1.5`)。
`comboDmg`(守弱時の追加ダメージ)を設計するとき、追加ぶんも含めた合計に1.5倍がかかる。
さらに**`comboDmg` は連撃1発ごとに乗る**(`k===0` のガードが無い)。
例: 虚式「茈」(攻150 連撃2 追加150) は守弱状態で `(150+150)×1.5×2 = 900`。
会心も乗るので、数値を測るときは `crit` を外して測ること。

---

## 6.549 演出が出ない端末(`prefers-reduced-motion`)と、その上書き設定

**「一部の人だけモーションが出ない」の原因はほぼこれ。** 実際に報告があり、原因もこれだった。

iPhoneの **設定 → アクセシビリティ → 動作 → 視差効果を減らす** がONだと、CSSの
`@media (prefers-reduced-motion: reduce)` が効いて次が全部消える:

- モーション(`.motion-fx` / `mv-*` / `emv-*`)
- 属性エフェクト(`.elem-fx`)
- 必殺技演出(`.special-fx`)・画面の揺れ(`.special-shake`)
- ※技名バナーは情報なので消さない(動きだけ止めて静かに出す)

**厄介な点**: 装備画面では標準モーションが正しく装備されて見える。所持データも正常。
JS側は`pickMotionForCat()`がちゃんとモーションを返し、DOMも作られる。
**CSSで`display:none`にされているだけ**なので、データを見ても原因が分からない。
見分け方は「ボスの必殺技で技名だけ出て派手な演出が出ない」。

### 上書き設定(`FX_PREF_KEY = 'mf_fx_pref'`)

端末の設定を尊重するのは正しいが、酔い防止などで視差効果を切っている人が
ゲームの演出まで全部失うので、ゲーム内から上書きできるようにした。設定パネル(⚙️)の「戦闘の演出」。

| 値 | 動き |
|---|---|
| `'on'` | `html.fx-on` が付き、端末がreduceでも必ず演出を出す |
| `'auto'`(既定) | クラスなし。端末の設定に従う(従来どおり) |
| `'off'` | `html.fx-off` が付き、端末に関わらず演出を減らす |

- **既定(`auto`)の挙動は media query 側に置くこと。** JSが動かなかった場合も従来どおりになる
- 演出を弱めるCSSを足すときは、**media query側(`html:not(.fx-on)`)と`html.fx-off`側の両方**に書く。
  片方だけだと「出す」または「減らす」のどちらかが効かなくなる
- `fxEffectivelyOn()` が「いま実際に演出が出るか」の唯一の判定。UIの案内はこれを見る
- 演出が出ない状態のときは、**モーション装備画面の先頭に案内とワンタップの修正ボタン**を出す。
  ここが「モーションが出ない」と思った人が最初に来る場所なので、必ず残すこと
- 端末側の設定は途中で変えられるので、`matchMedia`の`change`で表示を追従させている
- 検証: `scratchpad/fxpref_verify.js`(端末設定2通り × ゲーム内設定4通りの組み合わせ、
  パネル操作、再読み込み後の保持、装備画面の案内)

---

## 6.548 戦闘中ステータスの説明(`STATUS_DESCS`)

戦闘画面のステータスバッジは、文字だけでは何が起きているのか分からないものがある
(「炎王」「神核」が読めないという報告があった)。`STATUS_DESCS`に説明を書いておくと、
バッジがタップできるようになり(`ⓘ`が付く)、タップで説明が読める。

- バッジを作る`ps()`の**第6引数にキーを渡すだけ**で有効になる。渡さなければ今まで通りただの表示
- **説明は実装から起こすこと。** ここと実際の処理がズレると嘘の説明を配ることになる
- 対象: 神核・炎王・一撃入魂・絶対防御・回避・状態異常無効・攻弱・守弱・出血・気合い・自傷カード強化
- **名前付きの新しい仕組み(「◯◯」を付与、など)を足したら、ここにも必ず説明を書くこと。**
  カード説明文(`upgradeCard`)とセットで漏れやすい
- 表示は`showRelicTooltip`と同じ作りの単独パネル(`#status-tooltip`)。開くたびに前のを消すので重ならない

---

## 6.55 更新履歴の運用ルール(必ず守ること)

**ゲームに手を入れたら、毎回 `CHANGELOG` に1件足す。** バグ修正でも追加でも同じ。
タイトル画面左下の「最終アップデート」から見られる、プレイヤー向けのお知らせ。

- 配列の**先頭**に足す(新しい順に並んでいる)。日付が同じ日の変更は題名で分ける
- **細かい数値は書かない。** プレイヤーが読むものなので、何が変わったかが伝わればよい

| ✕ 書きすぎ | ○ ちょうどよい |
|---|---|
| 専用技は「魔王の雄叫び」(コスト20 攻35 氷結1 感電1) | 専用技は「魔王の雄叫び」です |
| HP倍率を1.5倍→1.2倍に下げました | 体力は控えめにしています |
| イベントを14種追加(全22種→36種) | イベントを追加しました |

数値を載せてよいのは、**プレイヤーが行動を変える判断材料になるとき**だけ
(ガチャの価格・確率、継承ショップの値段など)。

---

## 6.6 手札の並べ方とカード報酬

### 手札(`renderHand`)

**カードの大きさは「入る幅ぴったり」を毎回その場で計算する。**
以前は56px/62pxのような固定の段階サイズだったが、端数で1段階下に落ちてしまい、
5枚のときに6枚のときより小さくなる(56px < 62px)という逆転が起きていた。

```
1行: 幅 = min(基本サイズ, (中身の幅 - 隙間) / 枚数)
2行: 列数 = ceil(枚数/2)、幅 = min(5枚1行のときの幅, 上限, 列に入る幅)
```

- 実際の幅・高さはJSが`--hand-card-w` / `--hand-card-h`で渡す。縦横比は約1:1.43
- `data-density`(normal/lg/md/sm/xs)は**文字の大きさだけ**を切り替える。
  **density側にwidth/heightを書かないこと**(IDと属性セレクタの方が詳細度が高く、
  変数の指定に勝ってしまい大きさが変わらなくなる)
- 「5枚1行のときの幅」が上限。**6枚以上でこれより大きくはしない**
- 基本サイズの判定は`(min-height: 700px)`で80px。CSSの`@media`と同じ値にすること

実測(390×844): 1〜4枚 80px / 5枚 66px / 6枚以上 62px。
360×740では 80 / 60 / 60、430×932では 80 / 74 / 62。

### カード報酬(`createCardChoice`)

カード面には説明を出していない(名前と数値を大きく見せるため)ので、
**報酬の選択時だけカードの下に効果を表示**する。長押ししなくても比較できる。

幅は`flex-1 min-w-0 max-w-[104px]`で3等分。**固定幅にしないこと** —
104px固定にしたところ360px幅の端末で3枚目がモーダルからはみ出した。

デッキ一覧・カード削除・強化の画面は枚数が多く同じ手が使えないので、従来どおり長押し。

---

## 6.7 演出(モーション・属性エフェクト)

演出は**3つの層**に分かれている。混ぜないこと。

| 層 | 何 | 所持/装備 | 消し合うか |
|---|---|---|---|
| `.motion-fx` | プレイヤーが装備するカード演出モーション(16種) | ガチャで入手・装備画面で設定 | 対象ごとに1つだけ |
| `.elem-fx` | カードの中身から**自動で**出る属性エフェクト | 概念なし(誰でも出る) | モーションとは別レイヤーなので共存する |
| `emv-*` / `mv-*` | キャラ本体の動き | — | 1枚につき1回 |

### 属性エフェクト(`playElementFx`)

`elementOfCard()`が優先順に判定する。**炎上 > 氷結 > 感電 > 技名**。

- `burn` → 炎 / `freeze` → 氷 / `shock` → 電気
- 技名に`BEAM_NAME_HINTS`(砲・ビーム・レーザー・光線・フォース・キャノン・ブレス・波動・衝撃波)が
  含まれていればビーム
- 敵側は`enemyElementOf()`が技名だけから判定する(ファイアボール→炎、ホーリーサンダー→電気 など)

色は`ELEM_THEME`の`--ec`(主色)と`--eg`(グロー)の2変数だけで切り替えている。
属性を増やすときは`ELEM_THEME`に1行足して、CSSに`[data-e="新id"]`のブロックを足すだけでよい。

### 標準モーション(`basic_std`)

**ガチャを引いていない人でも演出が出るように**用意した、全員が最初から持っているモーション。

- `basic:true` かつ `cat:'all'`。`motionsOfCat()`が全場面の一覧に混ぜる
- `gachaMotions()`が`basic`を除くので、**ガチャの抽選にも天井の選択肢にも出ない**
- `ensureBasicMotion()`が`loadCosmetics()`のたびに所持と装備を補う。
  ただし**キーが既にある場面は触らない**ので、ユーザーが「なし」を選んだらそれを尊重する
- 1つで5場面を兼ねるため、出す場所と動きは`MOTION_BASIC_BY_CAT`が場面ごとに決める。
  `m.fxOn`/`m.move`は持っていないので、`playCardMotion`側で分岐している

### 敵のモーション(`playEnemyMotion`)

装備の概念はなく、行動の種類から自動で決まる。

- 攻撃 → 敵が`emv-lunge`で踏み込み、**プレイヤー側**にエフェクトが出る
- 強化 → 敵が`emv-rise`で伸び上がり、**敵自身**に光が立ちのぼる
- ブロック → `emv-brace`

敵の動きは`#enemy-monster-node`に当てる。プレイヤーと左右が逆なので符号を反転させた
`emv-*`を別に定義してある(`mv-*`をそのまま使うと敵が後ろに下がってしまう)。

---

## 6.75 必殺技演出(ボス・プレイヤー共通)

画面いっぱいの大きな演出。**ボスの技とプレイヤーのMRカードの両方**が同じ仕組みを使う。

- 本体: `playSpecialMoveFx(技名, 色, 攻撃か, grand, ラベル)`
- ボス: `playBossMoveFx(技名, trait, 攻撃か)` … ボス戦(`state.enemy.mode === 'boss'`)の敵の行動から呼ばれる
- プレイヤー: `playPlayerUltimateFx(カード)` … `playCard()`から呼ばれる。
  **MRかつコスト`PLAYER_ULT_MIN_COST`(30)以上**のカードだけが対象。
  MR全部にすると「極ガッツヒール」「ゴッドフォース」のようなコスト0の常用カードで
  毎ターン看板が出てしまい、かえって安っぽくなる

DOMは`#special-fx-layer`(エフェクト)と`#special-move-banner`(技名の看板)の2つだけ。
**名前に`boss`が付いていないのは、プレイヤー側でも使うため。**

### 仕組み: 「形」と「色」を別々に決める

技名を2回スキャンする。

1. `BOSS_FX_KINDS` → **形**(斬撃 / ビーム / 雷 / 花吹雪 / 氷 / 念動 / 闇 / 炎 / 聖光 / 風 / 地割れ / 連撃 / 闘気 / 衝撃 の14種)
2. `BOSS_FX_TINTS` → **色**(炎=橙 / 氷=水 / 雷=黄 / 聖=金 / 闇=紫 / 花=桃)

分けてあるので「フレイムキャノン = ビームの形 + 炎の色」「神モッチ砲 = ビームの形 + 聖の色」
「終焉の炎 = 闇の渦 + 炎の色」のような組み合わせが勝手にできる。
色の言葉が無ければ `BOSS_THEME_COLOR`(ボスごとのテーマ色)を使う。

- **判定は上から順、最初に当たったものが勝つ。より具体的な形を上に置くこと**
  (例: 「五月雨斬り」は rush の『五月雨』より slash の『斬』を先に当てたい)
- 手掛かりが無いときは、攻撃なら`impact`、それ以外は`roar`
- **技を増やしても、名前に手掛かりの語が入っていれば何もしなくても演出が付く**

### 必殺技だけの追加演出

`BOSS_ULTIMATE_MOVES`(Set)に載っている技だけ、**技名の看板・画面の揺れ・全画面の閃光**が付き、
エフェクトの物量も増える(`buildBossFxParts`の`big`)。
中身は `setEnemyIntentBase()` で `!e._xxxUsed`(1度きり)や `turnCount%N===0`(数ターンに1度)で
撃たれる大技と一致させてある。**必殺技を足したり技名を変えたら、このSetも直すこと。**

### 触るときの注意

- CSSは`<style>`内「ボスの必殺技演出」。**形ごとの見た目はCSS、部品を何個置くかはJS**(`buildBossFxParts`)と
  役割を分けている。数を変えたいだけならJS、見た目ならCSSだけ触ればよい
- **画面を塗りつぶさないこと。** 闇の霧と炎の火柱は当初いっぱいに広がって敵もキャラも見えなくなったので、
  霧は中央を透明に、火柱は細く数を増やす形に直した。派手さと視認性のバランスを崩さないこと
- 戦闘を離れるときは`clearSpecialFx()`を呼ぶ(`initMap`/`renderMapChoices`/`resetGameState`で呼んでいる)。
  忘れると演出が次の画面に残る
- **後片付けのタイマーは`__specialFxTimers`に持っておき、次の演出を始める前に必ず`clearTimeout`すること。**
  取り消さないと、続けて必殺技を撃ったときに「前の技の後片付け」が後から発火して、
  出たばかりの画面揺れや看板を消してしまう(実際にこの不具合を出した)
- `prefers-reduced-motion`では動きを止めるが、**技名の看板だけは消さない**(演出ではなく情報のため)

---

## 6.8 イベント

`showEvent()`の中に`normalEvents`(23種)と`rareEvents`(13種)の2つの配列がある。
**30%の確率でレア枠**が選ばれ、その中からランダムに1つ。

- `a:()=>{}` … 選択肢なしの即時実行型。`result`があれば結果画面、無ければそのまま次の階へ
- `choices:[{label,result,fn}]` … 選択肢型
- **`fn`や`a`の中で画面を自分で出した場合は`'skip'`を返すこと**。返さないと二重に画面が進む
- ライフを減らしたら必ず`updateUI()`→`if(state.player.hp<=0){gameOver(); if(state.battleEnded) return 'skip';}`
- ゴールドの加算は必ず`gainGold()`(直接`state.gold+=`だと継承ショップの倍率が乗らない)
- カード削除は`noRemove`(レジェンドの呪い)を必ず除外する

2026/08/04に14種追加(通常9・レア5)。600回発生させて全種が出ること・例外0件を確認済み。

---

## 6.98 ランキング

タイトルの「🏆 ランキング」。Firebaseが使えればオンライン、だめなら端末内(`mf_rankings`)に落ちる。
**相棒(種族)のタブは記録に実際に出てきた`monsterId`から作る**ので、種族を足しても何も書き足さなくてよい
(イブリースもそれで自動的に並んだ)。

| 定数 | 意味 |
|---|---|
| `RANKING_SHOW_MAX` | 一覧に出す件数(30) |
| `RANKING_FETCH_MAX` | オンラインから読む件数(300)。**増やすとFirestoreの読み取りがそのまま増える** |
| `RANKING_STORE_MAX` | 端末内に残す件数(120) |

- **絞り込んでも30位まで並ぶよう、読む件数は種族数ぶん多めに要る。** 100件だと1種族あたり
  十数件しか残らず、タブを押した瞬間に行数が減る
- 記録には`cardSet`(使った技セットのスキンid)も残す。一覧では`rankingCardSetBadge()`が
  「技」の印を付け、`title`にスキン名を出す。**印だけでは意味が伝わらないので、表の上に読み方を置いてある**
- `cardSet`を持たない昔の記録は、印が付かないだけでそのまま並ぶ

### 「📱 自分」のタブ

**オンラインの一覧は全プレイヤーの上位しか読んでいない。** 自分の記録がそこに届いていないと、
一覧にも相棒のタブにも一切出てこず「遊んだのに載っていない」状態になる(実際にそう報告された。
イブリースで1回遊んだのにタブが出ない、という形で表に出た)。

- `localRankingData`(この端末の`mf_rankings`)を、オンラインの一覧とは別に持っている
- 「📱 自分」を押すと、順位に関係なく自分の記録が全部出る。技セットの印もそのまま付く
- **端末内表示のときはこのタブを出さない。** 全体がそのまま自分の記録なので二重になる
- 相棒のタブは今までどおり**表示中のデータ**から作る。ここに自分のぶんを混ぜると、
  タブはあるのに押すと空、という悪い状態になる

## 7. コスメ(スキン・アクセサリ・オーラ)

localStorageキー `mf_cosmetics`。

```js
{
  ownedSkins: [],           // 所持スキンidの配列
  equippedSkin: {},         // {種族id: スキンid}
  ownedAccessories: {},     // {アクセサリid: {hue, rotate, offsetXPct, offsetYPct, scale}}
  equippedAccessories: { hat:null, mask:null, other:null, weapon:null },
  ownedAuras: [],
  equippedAura: null,
}
```

### スキン(30体・全画像登録済み)

各種族 SR3体 + SSR1体以上(モノリスとゴーレムはSSR2体)。ユーザー提供の高クオリティ一枚絵を丸ごと差し替える方式。
(当初検討していた「主要色検出による色置換」方式は**不採用**。画像支給前提に変更した)

種族ごとの命名例: モッチーSR「夜霧の/氷雪の/漆黒の宝玉」、SSR「聖天使モッチー」など。

### スキン専用の初期技(SSRスキン9体)

各SSRスキンに、その種族の看板技(初期デッキに1枚だけ入っている`*_b3`)の差し替え版が紐づいている。
**SSRは種族ごとに1体という決まりではない**。同じ`*_b3`を差し替える専用技が複数あってもよく、
装備画面の初期技リストは`getSkinStarterCards()`が種族のスキン全部を見て自動で並べるので、
SSRを1体足すたびに `SKINS` と `BASE_CARDS` の2箇所に足すだけでよい。

| 種族 | 通常 | 専用技(解放スキン) |
|---|---|---|
| モッチー | さくらふぶき 18/攻15 守弱2 | **天使の舞** 13/攻10 守弱2 回2(聖天使モッチー) |
| ゴーレム | 大パンチ 18/攻23 | **氷晶の鉄拳** 16/攻21 氷結1(氷晶装甲ゴーレム) |
| ゴーレム | 〃 | **スピリットパンチ** 12/攻28 攻弱2 クリC(スピリットゴーレム) |
| モノリス | おんぱ 12/攻12 攻弱2 | **翠玉の頁** 12/攻11 攻弱2 次ガッツ5(翠玉の禁書) |
| モノリス | 〃 | **魔王の雄叫び** 20/攻35 氷結1 感電1 クリE(魔王の魔典) |
| カワズモー | がっぷりよつ 21/攻22 守弱2 | **がっぷりよつ蒼** 15/攻17 守弱2 防8(宝玉の蛙王) |
| ガリ | レッドウィスプ 22/攻14 攻弱守弱2 | **深淵の焔** 22/攻12 攻弱守弱2 炎上2(深淵の陽衣) |
| ヒノトリ | 爆裂落とし 27/攻38 | **黄金雷撃** 27/攻32 感電2(黄金雷鳥) |
| ザン | 血踊 10/以降の攻撃に出血+1 消滅 | **溶岩の型** 13/炎上2 以降の攻撃に出血+1 消滅(溶岩装甲の忍) |

**解放条件はスキンの「所持」だけで、装備は不要**。装備を条件にすると見た目がSSR一択に
なってしまうため、意図的に切り離してある。装備画面の「初期技」欄から種族ごとに選び、
`mf_cosmetics`の`starterCards`(`{種族id: カードid}`)に保存される。

**重要**: 専用技は`BASE_CARDS`にだけ定義し、**`CARDS`には入れないこと**。
カード報酬・ショップ・イベントの抽選は全て`Object.values(CARDS)`から作られているので、
`CARDS`に入れるとスキン未所持でも入手できてしまう。
カード自体が`skinCardOf`(解放スキンid)と`replaces`(置換対象のカードid)を持っているので、
スキンを増やす時はカードを1枚足すだけでUIも自動で増える。

### アクセサリ(23種)

4ジャンル、各ジャンル1スロットずつ**最大4個同時装備**。

| ジャンル(内部id) | 内容 |
|---|---|
| 帽子 (`hat`) | 王者の兜 / 探偵帽 / ミニシルクハット / フローラルハット |
| メガネ・仮面 (`mask`) | ゴールドモノクル / ハイテクバイザー / オニキス仮面 / キャットアイマスク |
| その他 (`other`) | 浮遊式メカニカル・アーム・ユニット / 従者ミニオン(ちびゴーレム) / 紋章付き装甲プレート |
| 武器・小物 (`weapon`) | 聖なる盾 / エネルギーブレード / 従者スライム |

**座標システムは%ベース**(重要):
`pos: {種族id: {xPct, yPct, wPct, hPct, rotate}}` — キャラの表示サイズに対する割合で持つ。
これにより装備画面のプレビュー(160px枠)でもバトル画面(画面高さで67px〜88pxに変動)でも同じ位置に揃う。
ピクセル固定にすると画面ごとにズレるので**絶対に%ベースを維持すること**。

プレイヤーは装備画面で以下を自由にカスタマイズ可能(アクセサリごとに個別保存):
- 色相(`hue`、0〜359°、CSS `hue-rotate`)
- 角度(`rotate`、0〜359°)
- 大きさ(`scale`、30〜250%)
- 位置(`offsetXPct`/`offsetYPct`、プレビュー内をドラッグして調整)

**装備も調整値も種族ごとに別々に保存される**(`mf_cosmetics`)。

- `equippedAccessories`: `{種族id: {hat,mask,other,weapon}}`
- `accessoryTuning`: `{種族id: {アクセサリid: {hue,rotate,offsetXPct,offsetYPct,scale}}}`
- `ownedAccessories`: 所持の記録(旧形式の名残で調整値も残っているが、表示には使わない)

以前は全種族で共通だったため、体格の違う種族に持ち替えると位置がズレていた。
旧データは`migrateAccessoryData()`が**全種族に同じ設定をコピーする**形で引き継ぐので、見た目は変わらない。

**注意**: 調整値の書き込みは必ず`updateAccTuning()`を使うこと。
この関数は内部で`loadCosmetics`→`saveCosmetics`まで行うので、
呼び出し側で先に読んだ`c`を後から`saveCosmetics(c)`すると**変更が消える**。

### 形態ごとの位置(イブリース)

イブリースは通常形態(横に広い獣)と天使型(縦に長い六翼)で体つきが全く違うので、
**調整値だけ形態ごとに分けて持つ**。保存キーは `iblis`(通常形態)と `iblis@angel`(天使型)。

- キーを作るのは`accTuningKey(speciesId, formKey)`。**`accTuning`/`updateAccTuning`には
  種族idではなくこのキーを渡すこと。** 他の種族はキーが種族idのままなので何も変わらない
- **分けるのは位置・角度・大きさ・色だけ。** どのアクセサリを着けているか(`accSlots`)は
  種族ごとのまま。形態が変わるたびに装備そのものが入れ替わるのは分かりにくい
- **⚠️ 戦闘中は`applyFormVisual()`から`renderBattleAccessories()`を呼び直している。**
  ここを外すと、天使型なのに通常形態の位置のまま帽子が浮く
- 装備画面は`loadoutFormKey`(既定`'normal'`)で編集先を切り替える。切り替えボタンは
  `renderLoadoutFormSwitch()`が作り、形態を持たない種族では丸ごと隠す。
  種族タブを移ると`'normal'`に戻す
- プレビューの絵は`loadoutPreviewImg()`。スキン優先で、天使型なら`imgAngel`があればそれ
- 既存のセーブデータの調整値は**通常形態のぶんとしてそのまま残る**。天使型は既定値から始まる
- 他の種族に形態を足すときは`ACC_FORM_TUNED_SPECIES`を配列にするところから

**基準位置も形態ごとに持てる。** `data-accessories.js`の`pos`に `'iblis@angel'` を書けばそれが使われ、
無ければ`iblis`に落ちる(`accPos()`)。プレイヤーの調整値はこの基準位置の上に乗る。
通常形態(頭のてっぺん-30 / 顔-8 / 胴+8)と天使型(光輪-42 / 頭-34・やや右 / 胸-10 / 手+2)は
実際の絵を方眼に載せて測った値。**天使型は頭が小さいので帽子と仮面を4割ほどに縮めてある。**

**⚠️ `data-accessories.js`の`pos`には全種族ぶんのキーが要る。**
イブリースを公開したとき`iblis`だけ無く、全アクセが画面中央に既定サイズで出ていた
(`computeAccVisual`が既定値で握りつぶすので気づきにくい)。種族を足したら23件全部に足すこと。
`scratchpad/grid_normal.png` / `grid_angel.png` が測るのに使った方眼付きの絵。

### アクセサリを増やすとき

`data-accessories.js` の配列に1件足すだけでよい。装備画面もガチャの抽選対象も自動で追従する。

- 画像は**透過PNGを余白まで詰めて長辺180px**に揃えてある(既存も100〜215px。1件17〜57KB)
- `wPct`/`hPct` は**画像の縦横比に合わせること**。`renderBattleAccessories()`は`width`/`height`を
  直接指定していて`object-fit`が効かないため、比率がずれると絵が引き伸ばされる
- ジャンルごとの位置の目安: 帽子`yPct:-28`(角のように頭に載せるものは`-36`) /
  メガネ・仮面`yPct:-6` / 首飾り`xPct:0,yPct:4`(胸の中央) / 武器`xPct:-25,yPct:7`(体の横)
- 位置は全種族共通の仮値でよい。プレイヤーが装備画面で種族ごとに微調整できる

2026/08/04に9種追加(桜花のかんざし・妖魔の角・業火の角・紅薔薇のブローチ・翠玉のスカラベ・
蒼玉のペンダント・銀刃のレイピア・黒鞘のダガー・朧月の打刀)。
元画像は白背景のものと透過済みのものが混在していたため、白背景のものは
**外周から繋がっている白領域だけを塗りつぶす**方式で切り抜いた
(単純な白抜きだと真珠・銀の鎖・刃・宝石のハイライトまで消えるため)。

### アクセサリの位置調整(ドラッグ + スライドパッド)

位置の調整方法は2つあり、どちらも同じ`offsetXPct`/`offsetYPct`を書き換える。

1. **プレビューを直接ドラッグ**(`setupAccDrag`) — 動かした距離を基準サイズに対する%に直して保存
2. **スライドパッド**(`setupAccPad`) — プレビューの右に置いたアナログスティック

パッドを足したのは、**指でつまむとプレビューが指の下に隠れて見えない**ため。
離れた場所から操作できるようにした。

- 倒している間だけ`requestAnimationFrame`で動き続ける(押しっぱなしで移動する方式)
- 傾き量を**2乗**して速さに変換している。中央付近をゆっくりにして微調整しやすくするため
- `ACC_PAD_DEADZONE`(5px)以下の傾きでは動かさない。指の震えで勝手に動くのを防ぐ
- `ACC_PAD_LIMIT`(±150%)で行き過ぎを止める。押しっぱなしでどこまでも飛んでいかないように
- **閉じるときに必ず`cancelAnimationFrame`する**(`closeAccPicker`)。指を離すイベントを
  取り逃したまま閉じるとループが回り続けてしまうため

アクセサリを選び直すたびに`setupAccPad()`が呼ばれるので、`accPadHandlers`に前回のリスナーを
覚えておいて掃除している(`setupAccDrag`と同じ作り)。

### プレビュー枠のサイズは定数で持つこと(過去にやらかした)

アクセサリを重ねる場所は3つあり、それぞれ基準サイズが違う。

| 場所 | 枠 | 基準サイズ |
|---|---|---|
| 装備画面のプレビュー `#loadout-preview` | 144px (`w-36`) | `ACC_PREVIEW_REF` = 122 |
| アクセ選択のプレビュー `#acc-picker-preview` | 144px (`w-36`) | 同上(`ACC_PICKER_PREVIEW_REF`) |
| バトル画面 | 5.5rem / 4.2rem | `renderBattleAccessories()`が毎回計算 |

`computeAccVisual()`に渡す基準サイズと、中心座標(`枠 / 2`)が**枠のCSSと一致していないと、
位置も大きさも同時に狂う**。しかも調整値(`scale`や`offsetXPct`)が大きいほど誤差が拡大するので、
ユーザーが作り込んだ設定ほど大きくズレる。

実際、装備画面のプレビューを160px→144pxに縮めたときに`renderLoadoutPreview()`の中の
`refSize = 136` と `left = 80 + ...` を直し忘れ、**トップのカスタム画面だけアクセがズレる**不具合を出した
(アクセ選択画面とバトル画面は正しかったので気づきにくい)。
今は`ACC_PREVIEW_BOX` / `ACC_PREVIEW_REF`の2つの定数を両方の関数が参照している。
**枠のCSSを変えたら必ずこの定数も変えること。**

### ガチャの「ピックアップ」バナー

ガチャの単発/10連を選ぶ画面の一番上に出る枠(`buildGachaPickupBanner()`)。
中身は`GACHA_PICKUP`のidの配列を見て組み立てるので、**次に何か追加したらidを足すだけ**。

- SSRスキンは大きめのサムネ、アクセサリは横スクロールの小さいサムネで並ぶ
- 所持済みのものは薄く+チェックが付き、下部に「未入手 あと◯種」と出る。全部揃うと
  「コンプリートしました！」に変わる
- **ここに載せたSSRスキンは実際に確率が上がる**(6章「ピックアップ」参照)。
  バナーの「確率アップ中」の文言も`GACHA_PICKUP_SSR_RATE`から出しているので、
  確率を変えれば表示も追従する。アクセサリは確率アップの対象外(告知だけ)

### オーラ(5種・実装済み)

| id | 名称 | 実装した表現 |
|---|---|---|
| `aura_halo` | 光輪のオーラ | 楕円グロー+ふちの淡いリングがゆっくり明滅 |
| `aura_holy` | 神々しき後光 | 背後の金/紫の光+ゆっくり回る光条。キャラ本体は透過アルファ形状に沿ってdrop-shadowで発光 |
| `aura_flame` | 業火の足跡 | 足元の残り火+縦に伸びながら立ちのぼる炎の粒子6個 |
| `aura_rainbow` | 幻虹のオーラ | 回転する虹色リング(conic-gradient+maskのドーナツ)+きらめく粒子6個 |
| `aura_king` | 覇王の闘気 | 放射状のトゲトゲの闘気(repeating-conic-gradientで生成)が回転+明滅 |

画像素材は一切不要。**CSSのグラデーションのみ**で全7種族に使い回せる(当初SVGを想定していた覇王の闘気も、
`repeating-conic-gradient`+`mask`で同じ絵が作れるためDOMを増やさずCSSだけで実装した。いずれも独自実装で著作物不使用)。

**呼び出し漏れに注意**: プレイヤーのスプライトを組み立てる場所は
`renderBattleSprites()`(冒険の再開時)と`window.game.selectMonster()`(通常の開始時)の**2箇所ある**。
`renderAura()`が片方にしか無く、**普通に始めた冒険ではオーラが最後まで出ない**不具合が起きた。
`renderBattleAccessories()`を呼んでいる場所には`renderAura()`もセットで置くこと。

**構造**: `#battle-aura-fx`(バトル)と`#loadout-aura-fx`(装備プレビュー)を**静的に**置いてある。
中身は`.aura-layer-a` / `.aura-layer-b` / `.aura-p`(粒子6個)の固定8要素で、
どれを使うか・どう見せるかは全て`.aura-fx[data-aura="..."]`のCSS側が持つ。
JS(`renderAura()`)は**`data-aura`属性と表示/非表示を切り替えるだけ**で、DOMを動的に作らない。

- サイズは全て%指定なので、バトル画面(5.5rem/4.2rem)でもプレビュー枠(160px)でも同じ見た目になる
- オーラを増やすときは `data-auras.js` に1行足して、CSSに`[data-aura="新id"]`のブロックを足すだけでよい

### カスタム装備画面のレイアウト

縦に長すぎて何度もスクロールが必要だったため、**プレビューの左右に選択枠を寄せる**形に変更した。

```
[帽子][メガネ]   [ プレビュー144px ]   [オーラ]
[その他][武器]      モンスター          タップで
                   +アクセ+オーラ       一覧を開く
```

- 左列 `#loadout-accessory-slots` … **2列×2行**のグリッド。タップで従来のアクセサリ選択モーダル
- 右列 `#loadout-aura-slot` … 1枠だけ。`openAuraPicker()`で`#aura-picker-modal`(なし+5種)を開く

**左右の列は `flex-1 min-w-0 max-w-[104px]` で同じ幅**にしてある。プレビューだけ`shrink-0`なので、
画面幅が変わっても**プレビューは必ず画面の中央**に来る(360px〜430pxで中心ズレ0pxを確認済み)。
枠の大きさは画面幅に応じて41〜49pxに伸縮する。

オーラ枠の幅 `w-[calc(50%_-_3px)]` は「アクセ枠1マス」と同じ大きさになるよう計算したもの
(`gap-1.5`=6pxの半分を引いている)。**`renderLoadoutAuraSlot()`が`className`を丸ごと上書きするので、
HTML側のクラスを変えたらJS側も必ず同じに直すこと。**
なおTailwindの任意値でcalcを書くときは`_`が半角スペースに変換される仕様を使っている
(`calc(50%-3px)`と詰めて書くとCSSとして不正になる)。
- 以前あった画面下部のアクセサリ一覧・オーラ一覧のセクションは**削除済み**(`renderLoadoutAuraGrid()`は
  `renderLoadoutAuraSlot()`に置き換わった)
- プレビュー枠が小さいためオーラがはみ出す。`#loadout-aura-fx { transform: scale(0.78); }`で内側に収めている

画面の高さは 2159px → 1694px(約22%減)。

---

## 7.5 カードの見た目と操作

### 彫金デザイン(説明文は面から外した)

カード面は「コスト / 名前 / 大きな数値 / 会心・役割」だけにして、**説明文は長押しのポップアップへ移した**。
面の下40%を占めていた極小の説明(6〜8px)が無くなったぶん、名前と数値を大きくしている。

- **種族** … カードの地の色そのもの + 名前の帯(`--spBand`) + 下辺のライン = CSS変数 `--sp` / `--spBand`
  (7種族 + 共通 + 妨害の9通り。**`bg-zan`を足し忘れるとザンが共通色になる**ので注意)
  - 色は`createCardUI`が`c.mid`(種族id)を見て決める。**`mid`が無いカードは共通色になる**。
    初期デッキ用の`BASE_CARDS`は元々`mid`を持っておらず、初期カードが全部共通色になっていたので
    後から追加した。カードを足すときは`mid`の付け忘れに注意(共通の防御・ためるは意図的に付けていない)
- **役割** … 中央の光・背後の漢字の透かし・右下のラベル = CSS変数 `--tp`
  (中央は「暗い下地→役割色」の二層にしてある。種族の地の色が明るいと役割色が埋もれるため)
  (攻撃=赤 `攻` / 防御=青 `守` / 特殊=紫 `特` / 妨害=紫 `妨`。判定は `cardRoleOf()`)
- **レア度** … 枠の色と外側の光(`.rarity-*`)。右上のランク章の色もここで `--rankC/--rankB` として渡している

**注意点**:
- `.card`側で`box-shadow`を使ってはいけない。後方の`.rarity-*`の`box-shadow`に丸ごと上書きされて消える
- 透かしに絵文字(⚔🛡)を使うと`color`が効かず(カラー絵文字として描かれる)役割色で塗り分けられない。**漢字を使うこと**
- 役割色は`color-mix()`を使わず実色のrgbaで持たせている(古いiOSでも確実に出すため)
- 手札が増えると`data-density`が`sm`→`xs`と切り替わる。狭いほど会心/役割の行→ランク章の順に落として、
  **コスト・名前・数値だけは必ず読める**ようにしてある

### 長押しで説明を表示

**手札のカードは「長押し(200ms)＝ドラッグ開始」が既に埋まっている**ため、素直に長押しを割り当てられない。
そこで「長押ししてドラッグ状態にした後、**動かさずにそのまま離す**」(＝これまで何も起きなかった操作)に
説明表示を割り当てた。既存の操作は一切変えていない。

| 操作 | 結果 |
|---|---|
| タップ | カードを使う(従来通り) |
| 長押し → 敵へドラッグ | カードを使う(従来通り) |
| 長押し → 動かさずに離す | **説明を表示(新規)** |

手札以外(デッキ一覧・カード報酬・ショップ・修行)はドラッグしないので、500msの素直な長押しで出す。
指が12px以上動いたら中止するので、一覧のスクロールは妨げない。
カード要素には`__cardData`でカード本体を持たせてあり、ポップアップはそこから内容を引く。

---

## 7.6 難易度レジェンド

ベリーハードの上の最高難易度。

| 項目 | 内容 |
|---|---|
| 敵ステータス | **1.5倍**(HP・攻撃力とも。`startBattle`と`setEnemyIntent`の両方に倍率がある) |
| 最終スコア | **2.25倍** |
| 継承ポイント | **2.25倍**(スコア倍率と揃えている) |
| ダイヤ(階層到達) | 15階9 / 30階16 / 45階24 / 60階45 |
| 初踏破称号 | 170(63階なら220)。称号の格は「伝説」 |
| レジェンドボスラッシュ | ベリーハードと同じく解放される |

### 呪い(初期デッキに1枚)

`CURSE_CARD` … コスト0・効果なし・**デッキから取り除けない**。レジェンドでのみ初期デッキに1枚入る。

取り除けないことは`noRemove:true`で表現していて、以下の全てで除外している。
**カードを減らす処理を新しく足すときは、必ず`noRemove`を見て弾くこと。**

- 修行(削除) / ショップの削除
- 落とし穴イベント
- 合成(2枚を消費する)の候補
- 修行(強化) / ランダム強化の候補(強化しても無意味で枠を埋めるだけのため)

### 大ケガ

`BIG_INJURY_CARD` … コスト30・**ターン終了時に手札に残っていると自身に10ダメージ**(`handEndSelfDmg`)。

**レジェンド難易度かつレジェンドボスラッシュ中(61階以降)のときだけ**、
`addInjuryCards()`が通常のケガの代わりにこれを山札へ挿し込む。

自傷の判定は`handleEndTurn()`の冒頭、**手札を捨てる前**に行っている(捨てた後だと判定できない)。
ダメージは`applySelfDamage()`を通しているので、身代わり人形による軽減や死亡判定もそのまま働く。

---

## 7.7 カード演出モーション(15種)

カードを使ったときの演出。**5場面 × 3種 = 15種**。ガチャのSR枠で入手し、装備画面で場面ごとに選ぶ。

| 場面 | 発火するタイミング | 出る場所 | 候補 |
|---|---|---|---|
| 攻撃 | ダメージを与えたとき | 敵 | 斬撃 / 爆砕 / 連閃 |
| 防御 | ブロックを得たとき | 自分 | 六角障壁 / 石壁 / 光輪盾 |
| 強化 | 力・丈夫さ等が上がったとき | 自分 | 上昇気流 / 紋章 / 覚醒 |
| 弱体 | 相手に弱体を与えたとき | 敵 | 呪縛 / 侵食 / 亀裂 |
| 回復 | ライフが回復したとき | 自分 | 生命の雫 / 聖光 / 再生 |

**「一体型」**なので、エフェクト(`fxOn`)と**キャラの動き**(`move` = `mv-*`クラス)が1セットになっている。
定義は`CARD_MOTIONS`にまとまっていて、増やすときはここに1行足してCSSを書けばUIも自動で増える。

### 実装上の注意

- **キャラの動きは必ず`#player-visual-wrap`に当てる**。スプライト単体(`#player-visual`)を動かすと
  アクセサリとオーラが置いていかれる。wrap側の`idle`アニメより優先させるためID込みのセレクタにしてある
- **1枚のカードが複数の場面に当てはまることがある**(攻撃しつつ弱体を与える等)。同じ対象に重ねると
  後の演出が前を消すので、`__cardMotionUsed`で**対象(敵/自分)ごとに1つだけ**出すようにしている。
  キャラの動きも1枚につき1回だけ。呼ばれる順が 攻撃→防御→強化/回復→弱体 なので、攻撃カードなら攻撃の演出が残る
- エフェクトは敵/自分の**スプライト**(`ui.enemyVisual` / `ui.playerVisual`)に差し込む。
  ノード全体に差すとHPバーや攻撃予告の上にも出てしまう
- 未所持のモーションを装備データが指していても再生しない(`pickMotionForCat`で所持を確認している)

### 入手と装備

- ガチャの**SR枠(5%)を引くと、SRスキンかモーションが半々**で出る。被りは継承ポイント100(SRスキンと同じ)
- 100連天井の選択肢にもモーションが並ぶ
- 装備は`mf_cosmetics`の`ownedMotions`(所持)と`equippedMotions`(`{場面key: id | 'random'}`)
- **ランダム**を選ぶと、その場面で所持しているものから毎回抽選される。未設定なら演出なし(従来通り)

---

## 7.8 その他の画面

### 提供割合(ガチャ画面「📊 提供割合を見る」)

`showGachaRates()`。表示は`GACHA_RATES`から組み立てているので、**確率を変えれば表示も自動で追従する**。
ただし`rollGachaTier()`は合計値で正規化するため、**合計が100%から外れると表示と実際がズレる**。
確率をいじるときは必ず合計を1.00に保つこと。

### 更新履歴(タイトル画面左下「最終アップデート ▸」をタップ)

`CHANGELOG`配列を`window.game.showChangelog()`が表示する。**先頭が最新**で、先頭の1件だけNEWが付く。
新しい版を出したら配列の先頭に1件足すだけでよい。

### 敵スプライトの表示倍率

`ENEMY_SPRITE_SCALE`(trait → 倍率)。
敵画像は`6rem × 7rem`の**縦長枠**に`object-fit:contain`で収めているため、
**横長の画像は縦が余って小さく見える**。ビークロン(360×269の横長)がこれで、1.4倍を指定して解決した。
同様に小さく見える敵が出たら、この表からtraitと倍率を足す。

### 音量ボタンの重なり

`#global-volume-btn`は画面左上に固定表示(z-190)。カスタム装備画面の「← 戻る」と重なるため、
`openLoadout`で隠し`closeLoadout`で戻している。**左上にボタンを置く画面を増やすときは同じ対応が必要**。

---

### 記念配布などの一括配布

`UPDATE_GIFT_KEY` / `UPDATE_GIFT_AMOUNT`。localStorageのフラグを見て**1端末につき1回だけ**配る。
次回また配るときは、**キーの日付部分を変えて**金額を書き換えるだけでよい(キーを変えないと配られない)。
ログインボーナスのトーストと重ならないよう、0.9秒ずらして表示している。

---

## 8. 実装上の重要な注意点(ハマりどころ)

このセッションで実際に踏んだバグと、その教訓。

### TDZ(一時的死角)によるサイレント失敗

`const`で宣言した定数は、その行に処理が到達するまで参照できない。
ページ読み込み時に即実行されるIIFE(ログインボーナスのチェックなど)が、
後方で定義された`const DIA_STORAGE_KEY`を参照して**静かに失敗**していた。

→ 対策として、`window.game = {}` の直後(スクリプト最上部)に4つのストレージキーを全て定義済み:
```js
const DIA_STORAGE_KEY = 'mf_dia_progress';
const META_STORAGE_KEY = 'mf_meta_progress';
const COSMETICS_STORAGE_KEY = 'mf_cosmetics';
const GACHA_STORAGE_KEY = 'mf_gacha_progress';
```
**この位置から動かさないこと。**

### 非表示要素の`offsetWidth`は0になる

バトル開始時、まだ`display:none`の状態でスプライトのサイズを測ろうとして0になり、
アクセサリが非表示になるバグがあった。
→ 実測をやめ、CSSのブレークポイント(`max-height: 700px`)から表示サイズを直接計算する方式に変更済み。

### データファイルの差し替え漏れ

`index.html`だけ更新して`data-accessories.js`を古いままにすると、新コードが旧形式データを読んで
`NaN`になり「スライダーを動かしても何も変わらない」という分かりにくい症状になる。
**データ構造を変えたら、必ず該当データファイルも一緒に差し替えること。**

### try/catchが握りつぶすReferenceError(修正済み)

`playCard()`のクリティカル発動効果が**全て無言で失われていた**。

原因は`let anyCritThisAttack = false;`が`if(c.val>0||...)`ブロックの**内側**で宣言されていたのに、
判定はブロックの**外側**で行われていたこと。参照した瞬間にReferenceErrorになるが、
その一帯が`try{...}catch(e){console.error(...)}`で囲まれているため、
**画面上は何のエラーも出ず、ただ効果が発動しないだけ**という状態になっていた。

これで死んでいたのは以下すべて(ドローだけでなく全部):
`critEnergy` / `critBonusDmg` / `critWeak` / `critBlock` / `critDraw` /
クリティカルバッジ / モストの鉢巻 / 根性飴 / スケスケメガネ / 風神の刃

→ 宣言を関数スコープへ引き上げて解決。
**教訓**: 効果が「なぜか発動しない」時は、まずコンソールを見ること。
このプロジェクトは効果処理を細かくtry/catchで囲んでいるので、
1箇所の例外が握りつぶされて静かに機能が消えることがある。

### position指定の無い要素にz-indexは効かない

`.monster-sprite`には`z-index:10`が指定してあったが`position`が無かったため**z-indexが完全に無視されていた**。
そこにオーラ(`position:absolute`)を重ねたところ、オーラがキャラの手前に来て顔が塗り潰される、という症状になった。

→ `#player-visual`に`position:relative`を追加して解決。**重ね順を指定したい要素には必ずpositionも付けること。**

なお、`.aura-fx`は`z-index:5`でキャラ(10)より奥・アクセサリ(20)より奥に置いているが、
**業火の足跡だけは`z-index:15`でキャラの手前**に出している(背面だと足元の炎がキャラに隠れて見えないため)。

### 冒険終了後も自動セーブが復活してしまう(修正済み)

`gameClear()` / 敗北 / ギブアップはいずれも `clearAutosave()` を呼んでいたのに、
**クリア後にタスクキルすると冒険データが残っていて再開できてしまう**という不具合があった。

原因は「結果画面(モーダル)を出している間も、裏のバトル/マップ画面は表示されたまま」であること。
`doAutosave()` は「バトルかマップが表示中なら保存する」判定なので、

1. `visibilitychange`(hidden) → タスクキル時に `doAutosave()` が直接呼ばれる
2. `scheduleAutosave()` の保留中タイマー(400ms)が `clearAutosave()` の後に発火する

のどちらの経路でも、**消したはずの `mf_active_run` が書き戻されていた**。

→ `__autosaveDisabled` フラグを追加し、`clearAutosave()` で保留タイマーの停止と保存の無効化を行うよう修正。
新しい冒険が始まる `resetGameState()` で `false` に戻して再開する。
**冒険終了系の処理を増やす時は `clearAutosave()` を呼べばよい**(フラグも一緒に立つ)。

### 空divをアンカーにした重ね合わせは不安定

アクセサリ表示で、空の`<div>`を作ってその中に動的に`<img>`を追加する方式は
ブラウザによって描画されないことがあった。
→ 静的な`<img>`要素を4つ用意しておき、JSでsrc/styleを差し替える方式に変更済み。

---

## 8.9 管理者ページ(タイトル右下の「管理」)

新キャラ・新SSRスキン・新カードを本実装の前に実機で確かめるための道具箱。
**タイトル画面の右下にある小さな「管理」**を押して、合言葉を入れると使える(`?admin=1` でも開く)。

できること: ダイヤ／継承ポイントの増減・スキンやアクセの全解放・専用技セットの切り替え・
カード検索とデッキ投入・冒険中のステータス操作や階層ジャンプ・セーブ消去・
**まだ公開していない中身のお試し**(65階ボス「創造神テクモクレイン」との戦闘、新種族「イブリース」で開始)。

### **合言葉は「本当の防御」ではない**

このゲームはサーバーを持たない静的サイトなので、**合言葉の照合はブラウザの中でしかできない**。
本気で解析されれば必ず突破される。**「普通に遊んでいる人が押しても入れない」ための鍵**と割り切ること。
(そもそもlocalStorageを直接いじればダイヤは増やせるので、守るべき秘密は元から無い)

- 合言葉そのものはソースに置かない。**SHA-256のハッシュだけ**を`ADMIN_PASS_HASH`に書く
- 変えるときは新しい合言葉のハッシュを出して定数を差し替えるだけ(手順は定数の上のコメント)
- 解錠は`sessionStorage`に持つので、**タブを閉じれば聞き直す**

### 触るときの注意

- **Tailwind(CDN)に位置や表示を任せないこと。** CDNが届かない環境では
  `hidden` も `fixed` も `right-2` も効かない。実際に、
  ①管理者ページがタイトル画面に丸見えになる ②「管理」ボタンが画面の真ん中に浮く、
  の2つをこれで出した。**パネルの開閉は`style.display`、ボタンの位置はインラインstyle**で書いてある。
  クラスは色などの見た目だけに使うこと
- 入口ボタンは`#name-scene`(タイトル画面)の中に置いてある。**ゲームが始まれば一緒に消える**
- **⚠️ それだけだと冒険中に開けなくなる。** 実際に「冒険中でないと使えない」項目
  (創造神と戦う等)へ永久に届かない状態を作った。合言葉を通したタブでは
  **トップバーにも入口(`#admin-topbar-btn`)を出す**こと。表示切り替えは`refreshAdminTopbarBtn()`。
  ここも`style.display`で切り替える(Tailwindが届かないと普通の人にも見えてしまう)
- 「冒険中でないと使えない」項目は、できるだけ**その場で冒険を始めてしまう**方が親切。
  `startAdminRun(種族id, 難易度)`がタイトル画面の流れをそのまま通すので、これを使うこと
  (自前で`state`を組み立てると初期化もれが出る)
- 「強化してから戦う」の数値は**足し算ではなく「その値にする」**(ライフ300 / 力丈75)。
  毎回同じ条件で測れるようにするため。65階の実際の到達値
  (継承ショップ 力丈+15 / 通常遺物 力+71 丈+61 / ボス遺物 力+23 丈+23 → **力丈90超**)に寄せてある
- **⚠️ 底上げは呼ぶ順番が決まっている。**
  `adminStackDeck()`(デッキ)は**戦闘開始の前**、`adminBoostStats()`(ステータス)は**戦闘開始の後**。
  `startBattle` が山札を作り、かつ `regenEnergy` を0に戻すため。
  逆にすると「毎ターンのガッツ回復50」が戦闘開始で消える(実際に一度そうなった)

### レイアウトを目で確かめたいとき

サンドボックスからはTailwindのCDNに出られないので、そのまま撮ると
`hidden`も`flex`も効かない崩れた画面しか見られない。
`scratchpad/mkpreview.js` が**CDNの参照をローカル生成のCSSに差し替えた `__preview.html`** を作るので、
検証はそちらを開くこと(実機と同じ見た目になる)。CSSは `scratchpad/twbuild/` で
`tailwindcss -c tw.config.js` を回して作る。`__preview.html` は `.gitignore` 済み。
なお Playwright の `screenshot()` は `animations:'allow'` を明示すること
- 中身は押すたびに`showAdminBody()`で作り直す。表示中の数値が常に最新になる
- 検証: `scratchpad/admin_verify.js`

---

## 8.95 イブリース(プレイアブル種族)

63階のボスを、そのままプレイヤーの種族にしたもの。**戦闘中に2つの形態を行き来する**のが売り。
`IBLIS_ENABLED`(index.html)で選択画面への表示を切り替える(現在 `true` = 公開済み)。
管理者ページの「👼 イブリースを試す」からも直接始められる。

### 形態のルール

| 形態 | 与ダメ | 被ダメ | 毎ターンのガッツ回復 |
|---|---|---|---|
| 🌑 通常形態 | ×1.0 | **×0.9** | **+6** |
| 👼 天使型 | **×1.2** | ×1.1 | +0 |

- 数値は`IBLIS_FORMS`(index.html、`SELF_DMG_CARD_MULT`のすぐ下)に集約。**ここだけを直せばいい**
- 戦闘開始時は必ず通常形態。`startBattle`で`IBLIS_DEFAULT_FORM`に戻す(`atkBattle`と同じ扱いで、冒険をまたいで持ち越さない)
- 「溜める形態=通常 / 撃つ形態=天使」の往復がこのキャラの遊びになるよう、**ガッツ回復はあえて通常形態に付けてある**。
  天使型に「与ダメ+」と「ガッツ+」を両方付けると天使型に居座るだけのキャラになる

### ⚠️ 倍率は「実処理」と「予測表示」の4か所すべてに入れること

片方だけ直すと**「120と出ているのに132くらう」**という、気づきにくいズレになる。

| | 実処理 | 予測表示 |
|---|---|---|
| 受けるダメージ | `handleEndTurn`の敵攻撃ループ | `updateUI`の攻撃予告(`dmgVal`) |
| 与えるダメージ | `playCard`の`m`(倍率) | `calcPreviewDmg` |

`formTakenMult()` / `formDealtMult()` / `formEnergyBonus()` を必ず経由すること。
ガッツ回復は`energyRate`を読んでいる**3か所すべて**に`formEnergyBonus()`を足してある。

### カードの `formTo` と `formEff`

```js
formTo : 'angel' | 'normal' | 'toggle'   // 効果が全部終わったあとに変身する
formEff: { normal:{...}, angel:{...} }   // その形態で「使ったとき」だけ数値を差し替える
```

- **変身は必ず「効果のあと」。** 先に変身させると、変身技はいつも同じ形態でしか使われず、
  `formEff`が死ぬ。「いま通常だから通常ぶんの効果、そのあと天使型になる」が成立するのはこの順番のおかげ
- `formEff`は**上書き**であって加算ではない。`val:20, formEff:{angel:{val:33}}`なら天使型では33
- **`desc`は上書きしない。** descには`【天】`/`【通】`で両方の形態ぶんを書くこと。
  プレイヤーは変身する前に反対側の値も知りたい。カード面に出る数値だけが今の形態のもの
- **`formEff`で`cost`は変えないこと。** `clickCard`と`playCard`でコストを2回見ているので、ズレると事故る
- `formCard(c)`が「いまの形態で使ったらどうなるか」を返す。
  適用場所: `playCard` / `clickCard` / `createCardUI`(カード面) / `calcPreviewDmg`(ドラッグ中の予測)
- **カードの強化(`upgradeCard`)は`formEff`の中身も一緒に上げること。** 上げ忘れると、
  強化したのに片方の形態だけ据え置きになる。元データを壊さないようコピーを作ってから書き換える
- **`upgradeCard`の「説明文の作り直し」は`formEff`を持つカードでは走らせない。**
  作り直すと`【天】`/`【通】`の行が消え、片方の形態ぶんしか読めなくなる

### ⚠️ 使ったカードは「元のオブジェクト」を捨て札に戻すこと

`playCard`は`rawCard`(デッキの本体)と`c`(`formCard`の結果)を分けて持っている。
**捨て札/消滅置き場に戻すのは`rawCard`**。`c`を戻すと形態で書き換わった数値がそのカードに焼き付き、
2回目以降ずっとその値になる。

### 被ダメージ%カット(このキャラのために足した仕組み)

カードに`dmgCutPct` / `dmgCutTurns`を書くと、そのターン数ぶん**プレイヤーの受けるダメージが%で減る**。
いま使っているのは「天の慈悲よ示されよ」だけだが、他の種族のカードでも使える。

- 残りターン数は`startPlayerTurn`で1減る。**カードで代入するときに+1しないこと。**
  「2ターン」なら、使ったターンの敵の攻撃と次のターンの敵の攻撃のちょうど2回ぶんに効く
- ブロックを引く**前**に%で減るので、丈夫さやブロックと重ねて効く
- 倍率の例にもれず、**実処理(敵の攻撃ループ)と予測表示(`updateUI`の攻撃予告)の両方**に入れること
- バッジは`playerStatuses`、説明は`STATUS_DESCS.dmgCut`。強化したときの説明の作り直しにも足してある

### データ

- 種族: `data-species.js`の`iblis`(ライフ80 / ガッツ回復22 / `hidden:true`)。
  絵は通常形態(`img` 353x300)と天使型(`imgAngel` 320x231)の2枚。どちらも256色PNGで20KB前後。
  **63階ボスのイブリース(`data-enemies.js`)とは別の絵**なので、片方を直してももう片方は変わらない
- カード: `data-cards.js`の`iblis_b1〜b3`(初期デッキ)と`ib_*`32枚(N6/R7/SR7/SSR7/MR5)
- 遺物: `data-relics.js`の`ib_*`3種と`BOSS_RELICS_SPECIES.iblis`3種。
  形態に反応するぶんの処理は`setPlayerForm`(形態が変わった瞬間)と`startPlayerTurn`(毎ターン)に入っている。
  **毎ターンのガッツ回復を増やす遺物は、`updateUI`の「+○○」表示にも同じ行を足すこと。**
  ここを忘れて「実際は+32なのに表示は+22」を出した(検証で発見)
- 見た目: カードの色は`.bg-iblis`、天使型のあいだの光りは`#player-visual-wrap.form-angel`

### まだ公開していないスキン(`hidden`)

スキンに`hidden:true`を付けると、**ガチャ・提供割合・100連の「選んで入手」に出なくなる**。
公開するときは`hidden`を消すだけ。

- **ガチャまわりは全部`allSkinsOfRarity()`を通っている**ので、そこで弾けば一箇所で済む。
  管理者ページなど「未公開ぶんも欲しい」場面は`allSkinsOfRarity(rarity, true)`を使う
- **装備画面には「持っていれば」出す。** 持っていないものまで並べると、
  まだ出していないスキンの存在が枠として見えてしまう
- 提供割合の「全○種」は`allSkinsOfRarity('SSR').length`から出しているので勝手に追従する
- **いま未公開のスキンは無い**(ゲコウスグルとライオネルは公開済み)

### タイトルの「NEW」の帯(新スキンの告知)

`NEW_GACHA_SKIN_IDS`(index.html)に id を並べると、タイトルのガチャのボタンの真上に
絵と名前つきの帯が出る。押すとガチャが開く。**並べたスキンを全部手に入れると自動で消える**ので、
消す作業はいらない。次に新スキンを出すときは配列の中身を入れ替えるだけ。

- 描き直しは`updateTitleDiaDisplay()`に相乗りしている。タイトルに戻ってくる場所は
  (ガチャを閉じたとき・冒険が終わったとき含め)全部そこを通る
- **⚠️ 読み込みの途中では描けない。** `loadCosmetics()`は中で`ACC_SLOT_KEYS`(const)を使うので、
  スクリプトを読み終える前に呼ぶとTDZのReferenceErrorになる。ログインボーナスのIIFEからも
  `updateTitleDiaDisplay()`が呼ばれるため、`__newSkinBannerReady`で素通りさせ、
  `window.onload`のあとに1回だけ描いている(実際にこれで帯が出ず、検証で見つけた)
- **⚠️ 出し入れは`style.display`で行う。** Tailwindの`hidden`と`flex`はどちらも表示指定で
  どちらが勝つか読みにくい

### アップデート記念のダイヤ配布

`UPDATE_GIFT_KEY` / `UPDATE_GIFT_AMOUNT` / `UPDATE_GIFT_TITLE`(index.html)。
1端末につき1回だけ配る。**次に配るときはキーの日付を変えて金額と題名を書き換えるだけ**。
キーを変え忘れると、前回受け取った人には配られない。
- **未公開のあいだは更新履歴(`CHANGELOG`)に書かない。** プレイヤーから触れないものを載せても
  混乱するだけ。公開する回のコミットでまとめて1件足す(技セットや専用技の説明も一緒に)

### 形態ごとのキャラ絵と、変身の演出

- 通常形態は`img`、天使型は**`imgAngel`**(どちらも`data-species.js`)。差し替えは`applyFormVisual()`が行う
- **スキンも形態ごとの絵を持てる。** スキン側の`imgAngel`があればそれを使い、
  無ければスキンの`img`1枚で通す(判断は`getEquippedSkinImg()`)。
  **⚠️ スキン装備中に素の`imgAngel`へ落とさないこと。** 天使型になった瞬間だけスキンが消えて見える
- 実例: **ライオネル**(`iblis_ssr_01`)は通常形態が金の獅子の仔、天使型が六翼の大天使。
  専用の看板技 **「獅子と熾天」**(`iblis_b3_skin`)が付く。`formTo:'toggle'` のまま
  `formEff.angel` で中身だけ差し替え、通常=守って昇る / 天使=殴って降りる にしてある。
  **⚠️ ここから`toggle`を外さないこと。** イブリースの初期デッキで通常形態に戻れるのは
  この1枚だけ(光刃×2は天使型になる専用)なので、外すと天使型に入ったきり戻れなくなる
- 変身の演出は`playPlayerFormFx()`。白く飛ばす(0.18秒)→絵を差し替え→戻ってくる(0.44秒)、
  同時に輪2つと羽根12枚。色は天使型=金 / 通常形態=紫
- **⚠️ 全部で0.6秒に収めること。** ボスの形態変化(約1.7秒)と同じ長さにすると、
  **1戦闘に何度も起きるので毎ターン待たされる**。進行を止めない(awaitしない)ことも大事
- **⚠️ 1ターンに2回変身することがある**(反転の呼吸→天秤など)。
  演出を足す前に前回のぶん(`.pfchg-box`)を消すこと。放っておくと羽根が二重三重に出る
- **⚠️ `applyFormVisual()`では演出を出さない。** 戦闘開始やセーブ復帰でも呼ばれるため。
  演出を出すのは`setPlayerForm()`(silentでないとき)だけ
- 形態のバッジは`setPlayerForm()`の中で`updateUI()`を呼んで描き直している。
  呼び出し側任せにすると「絵は天使型なのにバッジは通常形態」という食い違いが一瞬出る
- 形態バッジは`playerStatuses`の**いちばん左に常時**表示。説明は`STATUS_DESCS.iblisForm`

### 種族を隠すしくみ

`speciesIds()`が`hidden`な種族を外す。選択画面・カスタム装備のタブ・称号一覧はこれを使う。
**アクセサリのデータ移行のような「全種族ぶん器を用意する」処理では素の`Object.keys(SPECIES)`を使うこと**(隠れている種族のデータが消える)。

### 火力の目安(力0・敵の防御0・会心なしで測った、ガッツ1あたりのダメージ)

| | 通常形態 | 天使型 |
|---|---|---|
| イブリース | 0.9〜1.5 | 1.5〜2.4 |
| ゴーレム(与ダメ1.25倍こみ) | 1.7前後 | — |
| ザン(出血0) | 0.8〜1.9 | — |

天使型の上のほうはゴーレムをかなり超える。とくに**二律の刃(R・コスト20で2.40)と
神光の裁き(MR・2.40)が頭ひとつ抜けている**ので、強すぎるようなら最初に見る場所。
そのぶん通常形態は0.9〜1.5とゴーレム以下に抑えてあり、往復して初めて成立するようになっている。

計測: `scratchpad/iblis_power.js`。
**会心を外して測るときは base と `formEff` の両方から `crit` を抜くこと。**
`formEff.angel.crit` が指定を上書きして会心が復活し、数値が2倍に見える(実際に2回踏んだ)。

### 残していること

- 解禁条件(「63階でイブリースを倒したら」など)。**種族の解禁というしくみ自体がまだ無い**。
  いまは最初から誰でも選べる

検証: `scratchpad/iblis_verify.js`(形態の土台) / `iblis_cards2.js`(32枚を両形態で1枚ずつ実測) / `iblis_relic_verify.js`(遺物6種) / `iblis_admin_verify.js` / `admin_fix_verify.js`(入口の不具合) / `iblis_power.js`(火力の目安)

---

## 9. デバッグ用のURLパラメータ

| パラメータ | 効果 |
|---|---|
| `?floor=61` | その階層から開始 |
| `?boost=1` | ライフ・力・丈夫さ・ガッツ上限を底上げ |
| `?boss63=1` | 63階ボスに即挑戦(ライフ300/力丈夫さ60/ガッツ回復+50/MRカード全取得/カード全強化) |
| `?resetCosmetics=1` | 所持スキン・アクセサリ・オーラ・装備状態とガチャ天井カウントを全消去。**初回のみお詫びダイヤ+200を付与**(`mf_reset_apology_claimed`フラグで管理) |

---

## 10. 次にやること

### 優先度高

1. **⑤ ガチャ演出の実装** — ✅ 実装済み(このセッションで対応)
   - `#gacha-overlay`(index.html、`<main>`内・modal-overlayの直後)を新設。CSSは`<style>`内「ガチャ演出」セクション(`.gacha-*`クラス群)、JSは「ガチャ演出」セクション(`playGachaAnimation`が入口)
   - 待機画面: 二重の魔法陣リング(`.gacha-ring-outer`/`-inner`)が常時ゆっくり反対回転
   - 抽選演出: 加速回転(`is-charging`)→カプセルが震えて光漏れ(`is-shaking`)→**上下の殻がパカッと開く**+パーティクル爆発+中身公開(`is-bursting`→`.gacha-reveal`)。タイムラインは`runSingleGachaReveal()`
   - レア度別の色/パーティクル量/画面フラッシュ有無/カプセルの見た目は`GACHA_ANIM_THEME`テーブルで管理(ハズレ〜オーラの6段階。SRは控えめフラッシュ、SSR/オーラは強いフラッシュ)
   - **カプセルの見た目もレア度で変わる**: SR=金(`cap-gold`、光沢が回転) / SSR=虹(`cap-rainbow`、色相が回転) / オーラ=虹+白く速く輝く(`cap-prism`)。中身が出る前から期待感が出るようにしたもので、10連のミニカプセルにも同じクラスが付く。テーブルの`cap`フィールドを変えるだけで差し替え可能
   - **カプセルは「揺れ」と「開閉」で担当を分けている**: 揺れ・拡大は`.gacha-capsule`、上下の殻が飛ぶ動きは`.cap-half`。1つの要素に両方のアニメーションを載せると`transform`が打ち消し合うので、分けたまま触ること
   - **背景は専用の不透明背景**(`#gacha-overlay`のCSS)。深い紫のグラデーション+星屑で「召喚の間」風。ここを半透明にするとタイトル画面が透けて演出が締まらないので、不透明のままにすること
   - オーラ枠のみ全画面リング拡散演出あり(`.gacha-ring-burst`、CSS maskで円形に、`border-image`はborder-radiusを無視して四角くなるため不採用)
   - 10連は`runTenPullGachaSequence()`が10個のミニカプセルを`.gacha-cap-grid`で順番に開き、最後に今回最高レアだけ`runSingleGachaReveal()`を「★ トリ確定演出 ★」付きで再生
   - **最高レアの1個はグリッドでは開けずに取っておく**(`heldIdx`)。10個全部の中身が先に見えていると、
     最後に何が出るか分かってしまって盛り上がらないため。取っておくマスは`.is-held`が付いて
     カプセルのまま光り続け(`gacha-held-bob`/`gacha-held-glow`)、トリ確定演出でようやく開く。
     マスは10個のままなので**曲との同期(`afterGrid`の計算)は変わらない**
   - **10連の演出は`bgm-gacha.mp3`と尺を合わせてある。** 曲の最後の「テン！」が鳴る位置を`GACHA_BGM_HIT_MS`(実測9280ms)に置き、
     トリ確定カプセルが**ちょうどそこで開く**ように各段階の長さを`runTenPullGachaSequence()`が逆算している。
     **曲を差し替えたら`GACHA_BGM_HIT_MS`を測り直すこと**(波形のピーク位置。PyAVでデコードしてRMSを見るのが早い)。
     段階の配分を変えたいときは`stagger`と`timing`の比率をいじれば、開封の位置は自動で合ったままになる
   - **ガチャBGMは画面BGMではない。** ガチャ画面にいる間はタイトルのBGMのままで、10連を引いた瞬間だけ
     `playGachaPullBGM()`が頭から流す(ループしない)。終了/スキップで`stopGachaPullBGM()`→`playMenuBGM()`。
     単発は尺が短いのでBGMを切り替えない。`wakeAudio`/音の見張り番(`watchedBgm`)は演出と同期した一発物なので`__bgmGacha`には手を出さない
   - 演出中は`#gacha-overlay`タップで`window.game._gachaSkip()`が呼ばれ即座に結果画面へ
   - 結果画面(`showGachaResults`)に天井カウント(次のSR/SSR・オーラ確定まであと◯連)を表示済み
   - アクセサリ/スキンは`item.img`があるので画像表示、オーラは`img`フィールドが存在しないため✨アイコンにフォールバック(3箇所: 結果一覧・単発演出の公開カード・10連ミニ卵の中身表示)
   - Playwrightで単発/10連/スキップ/SSR/オーラの各演出を実際にレンダリングして確認済み(Tailwind CDNがサンドボックス外からのみ読み込めるため、確認時は`tailwindcss` CLIでローカルビルドした代替CSSに差し替えてスクリーンショット取得。本番のGitHub Pagesは通常通りCDNから読み込むので影響なし)

2. **⑥ オーラの戦闘中表示** — ✅ 実装済み(このセッションで対応)
   - 5種類すべてをCSSのみで実装し、バトル画面と装備プレビューの両方に反映。詳細は「7. コスメ」のオーラ節を参照
   - Playwrightで5種類×バトル/プレビューを実際にレンダリングして確認済み

### 優先度中

3. ~~**初期遺物解放の中身を決める**~~ — ✅ 実装済み(候補12種から3択。詳細は「4. 継承ショップ」参照)。
   実際に何周か遊んでみて、選択肢が弱すぎ/強すぎと感じたら`STARTER_RELIC_POOL_IDS`を調整する
4. **アクセサリの種族ごとの座標調整** — 現状は全種族共通の仮値。装備画面のドラッグ機能で種族ごとに詰める
5. **スキンと連動した専用カード** — 「スキンによって専用カードを入れたい」という構想あり(未設計)

### 検討中(提案済み・未着手)

#### A. 強敵に「コンセプト」を持たせる案

**状況**: コンセプトは既に技構成の数字の中にあるが、**プレイヤーからは見えない**。
技の中身を読み取ると、9体すべてに性格が仕込んである。

| 強敵 | 技構成に現れている性格 |
|---|---|
| デュラハン | ブロックを張る技が3つ + HP400以下で「激怒」(力+15・block+34) → **鉄壁の騎士** |
| ヒノトリ | `revive` + 「再生の炎」(heal40・atkUp5) → **不死鳥** |
| メタルナー | ガッツ削り42/33/22 + handMinusが4技 → **封殺** |
| ドラゴン | 「様子を見ている」「あくび」でdmg0の休みが2つ、最大は92 → **溜めて特大** |
| ジョーカー | 全技にdrain + 「デスオーラ」(atkUp7・drainUp7) → **ガッツ泥棒** |
| ケンタウロス | 「ヤリ研ぎ」でatkUp10(once) → **尻上がり** |
| プラント | heal20・healSelf15・weak+vuln → **粘って吸う** |
| ビークロン | 「気合いだめ」(atkUp3・block20) → **溜めて殴る** |
| ヘンガー | 「攻撃体制」(atkUp5)、あとは素直に殴る → **純粋な火力** |

**案① まず「見せる」(一番安く、一番効く)**

ボスには既に **✨アイコンをタップすると特性の説明が出る仕組み**がある
(`#boss-effect-icon` / `showBossEffectInfo()` / `legendaryAura`・`legendaryWhim`)。
これを強敵にも広げ、名前つきの特性を表示する。**新しい仕組みはほぼ不要**(表示条件を広げるだけ)。

**案② 各強敵に「これ1つ」の仕掛けを足す**

データに1行足すだけで済むもの(既存の仕組みがそのまま使える):

| 敵 | 仕掛け | 使う既存機能 |
|---|---|---|
| ヒノトリ | 復活したら力+30 | `reviveAtkBuff`(フェニックスで稼働中) |
| ジョーカー | 攻撃時にケガを山札へ混ぜる | `injuryCards` |
| プラント | 与ダメの一部を吸収 | `healSelf` |

少し実装が要るもの:

| 敵 | 仕掛け | 狙い |
|---|---|---|
| ドラゴン | 溜めた次のターンは必ず特大技(今は完全ランダムで繋がっていない) | 休みターンが予告になり、防御を合わせる読み合いが生まれる |
| デュラハン | 毎ターン自動でブロック | 削りきる前に固め直される。一撃の重いカード/連撃で崩す相手になる |
| メタルナー | こちらのガッツ回復を下げ続ける | 長期戦にすると詰む。速攻を促す |
| ケンタウロス | ターンが進むほど力が上がる | 長引かせた側が負ける |

**進め方の案**: ①を先に → 31〜44階の3体(デュラハン・ヒノトリ・メタルナー)に仕掛けを1つずつ
→ 中盤・序盤は様子を見てから。

#### B. その他、提案したが着手していないもの


- **31〜44階の階層スケーリング** — 通常敵プールが`normal_late`の4種で固定なので、
  31階と44階で敵の強さが同じ。31階=×1.00 → 44階=HP×1.45/攻撃×1.2 くらいの案。
  ただし対象が14階ぶんしかないので効き目は限定的(46階以降には道中の敵がいない。「6.545」参照)
- **46〜59階の構成** — 休息所とボスの交互だけで道中の戦闘が1回もない。
  単調さが気になるなら、休息所の階に戦闘マスを混ぜる/ボスラッシュ帯専用の敵プールを足す、など
- **`manifest.json` が存在しない** — `<head>`で読み込んでいるのにファイルが無く、毎回404。
  iOSはApple独自のmetaタグ(全部揃っている)を見るので影響は小さいが、Android/PCのChromeでは
  アプリ名が`<title>`のまま(バージョン番号つき)になり、アドレスバーも出たままになる。直すのは5分
- **オフライン対応(Service Worker)がない** — 全体25MB(スキン7MB・敵2.9MB・本体1.6MB＋音楽)を
  毎回ネットワーク越しに取りに行っている。電波が悪いと起動が重い/開けない
- **マップ画面が無音** — 冒険中の移動画面だけBGMが無い
- **デッキ一覧の並べ替え・絞り込み** — 終盤は30枚以上になり探しづらい

### 保留・未確認

- ~~「全体サウンドをオン→オフ→オンにすると、メニューBGMが本来鳴らないはずの場面でも鳴ってしまう」~~
  → ✅ 解消。オフ→オンは`__currentBgm`をそのまま鳴らし直すだけになり、画面から推測し直さなくなった(上の「音が止まった状態からの復帰」参照)
- **敗北したあと、結果モーダルを閉じるまでバトルBGMが鳴り続ける**(勝利は勝利BGMに切り替わるのに、敗北だけ切り替わらない)。
  元からの挙動で、不具合として報告されたわけではないので触っていない。気になるなら`gameOver()`で`stopBattleBGM()`するか、専用BGMを足す
- タイトル画面の「継承ショップ」内に、ダイヤの入手履歴や統計を見せる画面があってもよいかもしれない(未検討)

---

## 11. 開発時のお願い

- 変更後は必ず**JSの構文チェック**を通すこと(このプロジェクトは1ファイルが巨大なので、構文エラーがあるとゲーム全体が起動しなくなる)
- ロジックを変更したら、可能な範囲で**実際に実行して検証**すること。このセッションでは、天井ロジックを10万回シミュレーションして「200連目でSR天井が消える」バグを発見できた
- iOS Safari(iPhone)で動作確認されることが多い。ホーム画面に追加したPWAとしても使われる
